{"pages":[],"posts":[{"title":"KV存储简介","text":"NoSQLNoSQL的含义是Not Only SQL， 是对不同于传统的关系型数据库的数据库管理系统的统称。 NoSQL一词最早出现于1998年，来源于Carlo Strozzi开发的一个轻量，开源的，不提供SQL功能的关系型数据库。该词在2009年于亚特兰大矩形的no:sql讨论会上被重新提出，被定义为“非关联性的”数据库，NoSQL更强调键值存储和面向文档数据库的优点。 NoSQL出现并在今年迅速发展的一个原因是进入互联网时代后，每天都将产生大量数据，以及与数据相关的查询，更新等操作。而传统的关系型数据库在高并发的使用情况下性能很低，而诸如Key Value Storage之类的NoSQL数据库则在这方面有着更加强劲的表现，具有更好的性能，可用性和可伸缩性。但NoSQL也存在一些缺点，例如它无法提供关系型数据库那样的ACID属性，只能提供相对弱一点的最终一致性模型，同时也无法提供更多的操作等等。 KV StoreKV Store指的是 Key Value Storage, 即键值存储系统。KV store是NoSQL运动的一部分，但实际上早在关系型数据库出现之前，KV存储的想法就已经得到了一定的应用，例如：使用hash表对数据进行索引。严格来说，所有的索引结构都可以看作是一种KV存储的体现，不同的是，早期我们通常只在内存中做这种事，而当现在KV真正成为一种存储系统，我们通常指的是将它应用于大规模存储中。Google发表的BigTable等一系列论文使得KV store逐渐走进了人们的视野之中。 KV存储，正如它听上去那样，是指使用&lt;Key, Value&gt;形式存储数据的一种形式。如果将每个key和 value的对作为一条记录，那么没有任何两条有效记录的key是相同的(我们使用有效是因为可能存在过期副本)。 KV存储和传统的关系型数据库的存储有很大的不同。关系型数据库中的基本数据单元也是一条“记录”， 但它通常是被预定义好的具有固定域的数据结构。例如，当我们想实现一个统计班上信息的数据库时，我们预先定义每条记录代表一位同学，这条记录的域包括：姓名，性别，年龄，家庭住址等等。在这种情况下，由多条记录组成的“表” 通常会表现为如下形式： id 姓名 性别 年龄 家庭住址 4123 小明 男 12 xxxx 192 小红 女 11 xxxx 9023 小刚 男 12 xxxx 如果我们想使用关系型数据库来实现它，我们可能需要多个表：Address, Age表，然后将一个student id中的address id关联到Address表上，诸如此类的。而在KV存储中，我们不需要表，也不需要为表设计关系，上面的一条记录就是value的值，通过索引student id就能得到一条记录的全部信息。 此外，对于KV存储而言，也不一定有如此规则的数据域。在KV存储中，每一条记录的value域通常被认为是无规则的字节流(当然也有一些固定域，例如在磁盘的偏移位置，但总的来说，并不如RDB(relation database)那样规则)。它们的每条记录具有相当的灵活性，是非常自由的数据模式，如下所示： key value K1 AAA, BBB, CCC K2 AAA, BBB K3 AAA,2,01/01/2015 K5 3, ZZZ, 5623 KV存储这样的存储特性带来了一些局限性，例如：不能根据value域的值来提取对应的key， 而在关系型数据库中，则能通过“提取年龄为12的记录”对应的SQL语句提取表中所有符合条件的记录，换言之，KV存储系统缺乏过滤功能。这意味着KV存储系统在所能提供的操作上会受到很大局限。 不过KV存储模型也有很多的好处，一是KV store对于高并发的需求支持得很好，二是KV store的扩展性较强。当存储数据量变大时，通过增加机器就能达到较好的效果。 参考与扩展资料[1] http://www.aosabook.org/en/nosql.html [2] https://zh.wikipedia.org/wiki/NoSQL [3] http://blog.marc-seeger.de/2009/09/21/key-value-stores-a-practical-overview/","link":"/2020/09/28/KV%E5%AD%98%E5%82%A8%E7%AE%80%E4%BB%8B/"},{"title":"LSM-Tree Compaction简介","text":"前言在前面已经提到，RocksDB采用LSM-Tree作为自己存储系统的主体部分, 从而以牺牲一部分读性能为代价提升了写性能。LSM-Tree的一个不足之处在于追加写的方式(Put, Update, Delete都是采用追加写的方式，Delete是写入一个删除标志)会导致存储系统中同一个key存在多个不同版本的value, 从而浪费空间。因此LSM-Tree的一个核心问题就是如何进行垃圾收集。LSM-Tree使用一种称为 Compaction 的机制进行垃圾收集，包括清除过期记录，合并具有相同key的键值对，收集闲置的空间等等。 Compaction算法本文先对LSM-Tree的Compaction算法做一些介绍，以便以后更好地分析RocksDB中使用的Compaction策略。 LSM-Tree的Compaction算法中，为人熟知的两种分别是 Size/Tiered Compaction 和 Leveled Compaction 。两种算法的核心做法都是将较低层的SST文件合并为较高层的SST文件，但是在合并时，具体的操作策略却有所不同，例如：Tiered Compaction倾向于将低一层的所有SST文件合并为一个新的SST文件后再加入到较高层，而Leveled Compaction在每次合并时只会将底层的一个SST文件选出来，与高层的文件合并。不同的合并策略导致了两种算法不同的行为，进一步导致了两种算法不同的性能表现。 这里引用参考文献[1]中的一幅图像来展示两种Compaction的不同： 上图先展示了两种Compaction策略的极端情形，左上角的log表示根本不进行Compaction，每次都写入到磁盘上，这样显然大大提升了写性能，因为写的时候根本不需要关心Compaction等操作，每次都是直接追加，但是坏处在于查询效率极其低下，原因是存储系统中可能存在一个key的多个value副本,只能进行线性查找；第二种极端情形就是右下角的 sorted array ， 它表示的含义是内存内容每次一刷新到盘上，就执行Compaction, 这样的好处当然是几乎时刻都能保持磁盘上的数据是最新且一致的，但是过度的Compaction会严重导致更新效率的下降。 在log和sorted array连成的这条线上，还分布着几个典型数据库，可以看出RocksDB，LevelDB更加接近于sorted array, 而Cassandra更加接近于log。几种数据库都采用了LSM-Tree的模式，但RocksDB和LevelDB采用了Leveled Compaction的策略，而Cassandra采用的是Tiered Compaction的策略。 Tiered CompactionCompaction策略Tiered Compaction可以说是最早出现的一种Compaction策略。它的要点有以下几个： SST文件的大小限制可变，通常来说，随着层次的增加，一个SST文件的大小会越来越大 LSM-Tree的每一层对SST文件数目进行限制，文件数目超过该限制就进行合并，且各层的数目限制相同 合并时第一层的 所有文件 合并为一个大的SST文件，放入到下一层。 一个经典展示Tiered Compaction合并过程的图如下所示： 从上图可以看出，假设每一层SST文件的数目限制默认为4，当该层数目达到4时，就会触发合并，将该层的4个SST文件合并为一个更大的SST文件，然后写入到下一层。层次越高，单个SST文件的容量也就越大。 性能分析Tiered Compaction的一个严重的性能问题就是其空间放大问题。所谓空间放大(Space Amplification), 就是指：在存储系统中，数据实际占有的空间比数据实际大小更大的情形 。Tiered Compaction的空间放大来源于它的Compaction执行。 当对一个Tier的所有SST文件进行Compaction时，原先的SST文件在Compaction完成之前是不会删除的，这就导致了在Compaction过程中，实际上存在SST文件的两个备份：原SST文件，合并中的SST文件。在最坏的情况下，占用的空间可以达到原先的两倍。 参考文献[2]中给出了测试Tiered Compaction空间放大的过程：通过持续向Scylla数据库以每秒3MB的速度写入数据，共持续3000s, 最后的结果应该是9GB左右，整个过程中磁盘使用量的情况如下所示： 在整个过程中，我们可以看见许多毛刺状的突起，这就是短视的空间放大。尤其是在大约2000s左右的时候，有一个巨大的突起。根据计算，此时实际数据量应该为6GB, 但图中显示却达到了12GB, 接近两倍。 第二个实验是重复写入一个400万条数据的集合，大小约为1.2GB, 通过重复写入15次来模拟数据更新，时间和磁盘占用的曲线图如下所示： 上述结果显示出来的空间放大极为严重，最多可达到9.3GB, 几乎是实际数据量的8倍。其原因在于尽管能通过Compaction消除本层的多个重复SST, 但不断进入的重复数据仍然会驻留在较底层，导致多个重复的key。 Leveled CompactionCompaction策略相比Tiered Compaction, Leveled Compaction有以下几个特点： 每个SST文件大小不变 每一层通过也通过限制SST文件的数目来触发合并(和 Tiered Compaction相同)，但是每一层的数目限制不同，通常是随层次增加而指数增长 每一层都维护唯一一个 run 所谓 run 就是指一堆数据的集合：要么是一个内部有序的SST文件；要么就是多个内部有序的SST文件，但是这些SST文件的key的范围互不重叠。 Leveled Compaction通过维护唯一一个run可以达到快速的查询的目的：如果该层的run是一个SST文件，那么直接采用二分查找即可；如果该层的run是多个范围互不重叠的SST文件，那么由于其范围互不重叠，可以找出包含目标key的SST文件(一定是唯一的），然后在该文件内部采用二分查找找到目标key。 合并过程下图很好地展示了Leveled Compaction的合并过程： 可以看出，不同于Tiered Compaction中单个SST文件容量的不断增长，在Leveled Compaction中，每个SST文件的大小都是相同的，每一层只是存储的SST文件的数量有所增加。减少SST文件的容量的好处在于在一个SST文件内部使用二分查找所占用的时间也会更少。 更加具体的Leveled Compaction的合并过程可以参考[3]。 Leveled Compaction和Tiered Compaction的另一个显著不同，正如在[3]中提到的那样，在于它的合并过程并不是将低层次的所有SST文件合并为一个新的SST文件然后刷新到高一层中，而是选取低层次的一个SST文件，将它与高层次中与其key范围有交叠的那些SST文件合并。这个过程能保持Leveled Compaction中要求的“每一层维护唯一一个run”。因为当一个SST和高一层与它有交叠的SST合并时，这些SST在序的关系上来看一定是相邻的，从而可以看作是将该SST中的新数据更新到高一层的多个SST中，这个过程并不会改变这些相邻SST的边界关系。 性能分析Leveled Compaction的一个严重问题就是其写放大，所谓写放大是指在向磁盘或SSD写入数据时，实际写入的数据量是实际数据量的数倍。 Leveled Compaction写放大的原因在于其每次Compaction都会将低层次的一个SST文件与高层次中和它的key范围有重叠的所有SST文件合并，这个重叠的SST文件的数目可能会非常巨大，从而导致重写的数目非常巨大。尽管Tiered Compation也有写放大，但是Leveled Compaction的写放大更加严重。 不过实际应用中，Leveled Compaction的写放大并没有那么严重，一个合理的解释源于程序的时间局部性：一个新写入的数据很有可能在短时间内被再次修改。这部分数据通常存储在内存或L0中，它们很少向更高层次移动，从而在Compaction时，包含它们的SST和高一层次的SST的交叠较少，从而减少了写放大。 Leveled Compaction 和 Tiered Compaction的比较上面的内容已经包含了部分两种方式的比较，下面以[1]一个例子来说明： 上图可以看出两种Compaction方式的一些不同，首先，Tiered的方式中，SST 4到达L1之后，触发合并，和1,3,7合并成为一个更大的SST文件，刷新到L2，此时L2中实际上是不止一个run的，因为2,5,6,8这个SST和1,3,4,7这个SST的key范围是有重叠的。相比之下，Leveled的方式最后将2,5,6,8也进行了合并，形成了唯一一个run。 上述两种不同的方式导致了在读效率上的不同。对于Tiered 而言，同一层中不同的run之间的key范围有重叠，这意味着在读的时候可能需要读取多个SST文件；而Leveled Compaction则不会有这个问题。因此Leveled Compaction有更好的读性能 在写性能上，Leveled Compaction的更新需要更加频繁，具有严重的写放大问题，而频繁的写会严重损耗SSD和磁盘的寿命。 参考文献[1] Niv Dayan , Dostoevsky: Better Space-Time Trade-Offs for LSM-Tree Based Key-Value Stores via Adaptive Removal of Superfluous Merging [2] https://www.scylladb.com/2018/01/17/compaction-series-space-amplification/ [3] https://github.com/facebook/rocksdb/wiki/Leveled-Compaction [4] https://www.scylladb.com/2018/01/31/compaction-series-leveled-compaction/","link":"/2020/09/16/LSM-Tree-Compaction%E7%AE%80%E4%BB%8B/"},{"title":"Modules and Homorphism","text":"前言本文主要介绍模的一些基本概念和结果","link":"/2020/08/02/Modules-and-Homomorphism/"},{"title":"RocksDB基本结构","text":"前言RocksDB是一种快速存储系统，改进于 LevelDB 并在一些地方做了新的优化和支持，例如：支持多线程读写，支持多线程的Compaction操作等等。本文将对RocksDB整体的构造布局做一些简单的介绍。 RocksDB基本结构RocksDB的基本结构可以直接用下图进行展示： 上图按照硬件的存储层次将 RocksDB 的结构分为内存和磁盘上两大部分。内存部分主要由memtable 和 immutable memtable 组成。磁盘中的部分主要由多级文件结构组成。 上图还展示了 RocksDB 读写时操作，当RocksDB读数据时，会按照memtable-&gt; immutable memtable -&gt; block cache -&gt; SST file(L0 -&gt; L1 -&gt; …) 这样的顺序进行查找。当写数据时，会先同时写入WAL(Write Ahead Log) 和 memtable, 前者的作用是保障一致性，后者则是作为存储系统的写缓存。memtable空间满后会转换为immutable memtable并磁盘刷新。 在逻辑上，LSM-Tree是RocksDB 的存储主体，下面介绍LSM-Tree。 LSM-TreeLSM-Tree是RocksDB的基本组成部分之一，其主要作用是通过批量的存储技术来减少随机写导致的高昂的写代价。我们都知道，对于磁盘等存储系统，其读写时间有相当一部分都花费在寻道和旋转上。对于随机读写，会导致寻道和旋转频繁发生，从而降低读写性能。好的解决办法是将写内容先保存在内存中，因为内存的随机读写性能高，然后奖随机写转换为顺序写，利用磁盘的顺序写来提升写性能。 LSM-Tree就是通过批量存储的方式规避了随机写的问题，LSM的全称是Log-Structured Merge Tree, 在 RocksDB中，这是一种层次性的数据结构，类似于一棵树。当要写入数据时，RocksDB会先将数据写入到内存中一部分区域，当该区域中数据越来越大直到达到一个阈值时，就会生成一个称为 immutable memtable 的文件(意为不可修改的)并且 flush 到磁盘上(称为刷盘)，形成SST (sorted string table)文件。但是刷盘并不是随意地刷新到某个位置，正如上面所说，LSM是一种多层次的结构，此时刷盘会刷新到内存下磁盘的”第一层”。处于磁盘中的树会 周期性 的触发 merge 操作，这是一种将小的“树”合并为大“树”的操作，当该层合并后导致容量到达一个阈值时，就又会进行刷盘，刷新到更下面的层。该过程可以用下图简单地表示出来： 在上图中，处于memory的C0层满了之后(当然并不是以树的数据结构组织的)，就被刷新到C1层，成为一棵子树，它会和原先就驻留在C1层的其他子树一起形成一棵更大的“树”，这个过程就是合并(Merge), 但合并不是简单地将文件拼接在一起，而是一个更加复杂的过程。但SST文件本身内部有序的组织方式在一定程度上减少了Merge的复杂度。 当读数据的时候，RocksDB会先从内存中的 memtable 开始找，找不到就到 immutable memtable 中找，然后到磁盘的 L0, L1,… 中找，这个过程被称为读放大。RocksDB还有一个被称为manifest 的日志文件，它记录了 SST 的修改日志。 LSM-Tree 全称为 Log-Structured Merge Tree , 但它并不是一棵树，甚至不是一个具体的数据结构，而是一种存储设计上的概念，例如，在LSM-Tree的memtable部分，RocksDB采用的是SkipList 这样的数据结构来存储；而在磁盘部分，RocksDB则采用有序的文件集形式组织数据，而不是传统的B+树的形式。这些文件集的层次体现在写入时间或合并时间上，而并不体现为在磁盘上的物理组织方式。除此之外，LSM-Tree还具有以下几个特点： 写采用追加写的方式。当写入数据时，都是将数据写入一个或几个新的文件，采用顺序写入的方式并且不允许修改其他的文件。这样的一个问题就是当修改数据时，就会在数据库中产生一个键值对的多个副本，造成空间的浪费。 数据按照key排序。LSM-Tree通过Merge操作合并不同的SST文件，通过事先对数据采用有序组织的方式可以使得合并更加高效。同时使用有序组织也可以利用 二分查找 更高效地找到一个key在 SST 文件中的位置。 内部的Garbage Collect。上面已经提到了，追加写的方式容易导致一个数据的多个副本同时存在，造成空间的冗余，而RocksDB内部提供了Compaction操作作为一种垃圾收集的机制。包括对数据进行删除，定期淘汰一些过期的数据，同一个key的value合并等操作。 从上面的叙述不难看出，LSM-Tree实际上是牺牲了读性能(多次读取memtable, SST文件)，而提升了写的性能(将随机写转换为批量的顺序写)。原因在于读性能还能通过软件进行优化，而写性能更多地依赖于磁盘的IO。 参考资料[1] https://github.com/facebook/rocksdb/wiki/RocksDB-Tuning-Guide [2] https://github.com/facebook/rocksdb/wiki/Leveled-Compaction","link":"/2020/09/14/RocksDB%E5%9F%BA%E6%9C%AC%E7%BB%93%E6%9E%84/"},{"title":"RocksDB的编译安装和Benchmark运行","text":"前言今年的FAST会议收录了一篇关于RocksDB数据库测试负载改进的文章：Characterizing, Modeling, and Benchmarking RocksDB Key-Value Workloads at Facebook。 这篇文章公布了一些 Facebook 统计的一些负载特征，这种商业级的负载还是有很大的参考价值的。出于阅读论文和进一步工作的原因，需要安装RocksDB数据库，在此将这个过程中遇到的问题和解决方法列举出来，以供参考。 安装过程本次安装使用的环境如下： 1234CPU: 4 * Intel(R) Core(TM) i5-7300hq CPU @2.50GHzCPUCache: 6144KBMemory: 16GBOperating System: CentOS7 这套硬件是我自己的笔记本硬件，算很老的配置了，但做做测试还是可以的。 需要的依赖： 1zlib, snappy, lz4, zstd 安装选定自己要下载的github版本，然后： 1git clone -b 版本号 https://github.com/facebook/rocksdb.git 例如我下载的是v6.8.1, 就是： 1git clone -b v6.8.1 https://github.com/facebook/rocksdb.git 然后进入rocksdb文件夹下创建build文件夹： 1mkdir build &amp;&amp; cd build 使用cmake进行编译: 1cmake .. -DCMAKE_INSTALL_PREFIX=/usr/local/rocksdb DCMAKE_INSTALL_PREFIX设置的参数是安装路径，这里设置为单独的rocksdb文件夹方便管理。如果希望生成的文件能 debug, 读取符号表等操作，需要将DCMAKE_BUILD_TYPE设置为Debug, 如果希望使用 RocksDB 自带的 Benchmark ， 需要加上 ZLIB 等选项。如下所示： 1cmake .. -DCMAKE_INSTALL_PREFIX=/usr/local/rocksdb -DWITH_SNAPPY=1 -DWITH_LZ4=1 -DWITH_ZLIB=1 -DWITH_ZSTD=1 -DCMAKE_BUILD_TYPE=Debug 然后分别make和 make install即可。 可能遇到的问题可能遇到的问题大多与依赖库有关，因此需要在安装rocksdb之前将上面提到所需要的依赖库先进行安装。安装可以使用源代码编译安装，也可以使用yum install命令安装： 12345678snappy: sudo yum install snappy snappy-develzlib: yum install zlib zlib-develbzip2: yum install bzip2 bzip2-devellz4: yum install lz4-devel 在安装完成依赖库之后，运行rocksdb的cmake时，可能出现提示在CMAKE_MODULE_PATH找不到Findzlib.cmake等文件的错误(其他文件同理), 此时只需要将Findzlib.cmake文件拷贝到rocksdb/cmake/modules文件夹下即可。Findzlib.cmake文件可以从网上找一个，也可以用RocksDB原来版本cmake/modules文件夹下的同名文件。 如果有兴趣写一些简单的测试程序或调用rocksdb的话还需要将安装的 rocksdb 加入到动态链接库的路径/usr/local/lib中: 12cp /usr/local/rocksdb/include -r /use/local/includecp /usr/local/rocksdb/lib64/librocksdb.so.6.8.1 /usr/local/lib 按照规范，还需要创建两个软链接： 12ln -s librocksdb.so.6.8.1 librocksdb.so.6ln -s librocksdb.so.6 librocksdb.so 最后引入路径：export LD_LIBRARY_PATH=/usr/local/lib， 如果不想每次都敲这么一行命令只需要在~/.bash_profile中加入这么一句即可。 Benchmark运行在build文件夹下运行db_bench即可运行基准测试程序。RocksDB提供了很多基准测试程序，使用./db_bench --help可以查看各个参数的意义。例如： 1234--benchmarks=&quot;...&quot; 基准测试的类型，例如fillseq, readrandomwriterandom等等--num 总的键值对个数--reads 读操作次数--writes 写操作次数 运行完毕后会打印相应的统计信息。上面提到的论文还实现了一种新的负载MixGraph， 通过修改--benchmarks=&quot;mixgraph&quot;就可以调用。github 上RocksDB项目的wiki提到了该负载下一种参数的设置方法： 12345678910111213141516171819202122232425./db_bench --benchmarks=&quot;mixgraph&quot; \\-use_direct_io_for_flush_and_compaction=true \\-use_direct_reads=true \\-cache_size=268435456 \\-keyrange_dist_a=14.18 \\-keyrange_dist_b=-2.917 \\-keyrange_dist_c=0.0164 \\-keyrange_dist_d=-0.08082 \\ -keyrange_num=30 \\-value_k=0.2615 \\-value_sigma=25.45 \\-iter_k=2.517 \\-iter_sigma=14.236 \\ -mix_get_ratio=0.85 \\ -mix_put_ratio=0.14 \\-mix_seek_ratio=0.01 \\-sine_mix_rate_interval_milliseconds=5000 \\-sine_a=1000 \\-sine_b=0.000073 \\ -sine_d=4500 \\--perf_level=2 \\-reads=420000000 \\-num=50000000 \\-key_size=48 该设置实际上模拟了ZippyDB的实际工作负载。更细节的信息可以查看wiki: https://github.com/facebook/rocksdb/wiki/RocksDB-Trace,-Replay,-Analyzer,-and-Workload-Generation和这篇论文。 值得注意的是上面涉及的参数keyrange_dist_a到 keyrange_dist_b在较老的版本中是没有实现的，不知道是作者忘记了还是因为什么，建议下载较新的rocksdb版本，例如v6.8.1","link":"/2020/08/31/RocksDB%E7%9A%84%E7%BC%96%E8%AF%91%E5%AE%89%E8%A3%85%E5%92%8CBenchmark%E8%BF%90%E8%A1%8C/"},{"title":"SkipList简介","text":"前言SkipList是一种集快速查找，插入，删除等功能于一身的内存数据结构,广泛应用于RocksDB，LevelDB等数据库的内存部分。通过学习RocksDB的机会，我了解到了这种以前从未听说过的数据结构。本文将简要介绍关于SkipList的一些基本知识，后续可能还会加入一些实现和优化 概述多层次的 有序 链表。最底层是一个包含了所有元素的有序链表，高层则是由较低层次的子序列形成的有序链表。高层次的链表相比于低层次的链表跳过了一些元素，所以被称为跳表。一个简单的跳表示意图如下所示： 跳表的查询效率可以达到$O(\\log n)$, 最一点足以与平衡二叉树媲美，但skiplist的实现复杂度低于平衡二叉树。同时，由于平衡二叉树存在大量的平衡-旋转操作，当大量的数据插入或修改时，会对平衡二叉树的性能造成巨大的影响，甚至导致阻塞。 [1] 的作者提出了一种独特的视角来理解skiplist: 将skiplist去掉一些重复边之后旋转，可以将skiplist看作是一棵二叉树： 上面的右图并不算是严格的Binary Search Tree, 因为左子树的数据并不全是小于根节点的数据。但查询过程有一些类似，当我们想要查询30时，我们发现30大于7，因此到7的右子树里面查找，然后发现30小于53，所以到25的左子树中查找；进一步发现25小于42，再到左子树中查找，最后找到30。如下图所示： 上述查找过程体现出了BST和SkipList在组织方面的一些不同，一个数据在SkipList中可能有多个备份，而高层中的数据的作用是用于界定查找范围。 从二分查找到SkipList[2]中也提出了一种有趣的思考SkipList的方式，就是从二分查找的角度来思考。举例来说, 如果我们有一个有序的数组：1，3，19，23，57，62。那么要从里面查找某个元素，例如23，通过二分查找来是非常高效的。但是对于同样排列的一个有序链表而言，二分查找却不能生效。原因在于链表并不支持随机的下标访问。通过双指针法倒是能够在每次获取到中间位置的节点，但是遍历仍需$O(n)$的代价，总共需要$O(n\\log n)$的代价。 虽然无法直接通过“下标”的方法获取得到中间位置的节点，但我们可以将该节点保存下来，将其作为最高层，然后对于左半部分，获取其中间节点的位置并保存下来；右半部分也做同样的操作，形成第二层；依次进行下去，最后得到完整的“树”型结构。这就是跳表的雏形： 当然这并不是跳表的最后形式，在跳表中还有一些随机化的因素，所以最后的跳表并不一定完全遵循上面的形式，同时还存在一些可能的优化。比方说，假设我们要查询7这个节点，那么我们先查询整个表的“中间节点”， 这可以通过读取最高层的元素得到；我们发现是4，7 &gt; 4, 所以向右半部分查找。此时我们需要查找右半部分的中间位置，这个值怎么得到呢？按图中来看，我们需要从第二层开始向右移动3个位置才能得到6这个节点，这个移动本身可能是非常耗时的，尤其是当查询达到最底层时，需要移动到该链表非常靠后的位置时，这个最坏情况需要$O(n)$ 的复杂度。 一个解决办法就是在两层的相同元素之间添加自上而下的指针。例如图中最高层的4到第二层的4需要添加一个指针，这样当我们需要获取右半边的中间元素时，就可以使用：4-&gt;4-&gt;6这样的序列而不是2-&gt;4-&gt;6这样的序列。 基本操作一些术语规定包含了所有数据的有序链表称为最低层，往上高度越高。 一个数据的 height 是指整个skiplist中包含该数据的节点所在的最高的高度。例如在上面包含7,11,22,25这副图中，11的高度是1, 15的高度是2，25的高度是3。 插入节点除非特别说明，否则我们都认为插入节点时该数据在SkipList中没有出现。为了插入一个节点，我们需要对这个新插入的节点分配一个高度，然后从该高度开始向下的每一层都插入相同数据的节点。 新插入节点的高度由如下概率决定：$p(n)=2^{1-n}$， 其中n表示高度。该分布意味着高度越高，节点处在这个高度的概率越小。 [1]提出了一种快速得到上述分布的方法： 1234567unsigned int generate_level() { unsigned int n = 0; while (coin_flip()) { ++n; } return n;} ​ 其中 coin_flip()函数实现了抛硬币的功能，它以0.5的概率返回true, 0.5的概率返回false。 就插入方法而言，[1]中使用了递归的插入方法，[2]中使用的则是求出每层中要插入节点的后继的方法然后进行插入。但总地来说，插入方法还是比较单一的：通常使用自顶向下的插入方法，在插入节点的高度位置，先找到该节点的前驱和后继进行插入，然后通过前驱的向下指针索引到下一层的对应位置，从该位置开始向后查找前驱和后继，如此循环，直到最后在最底层完成插入。 删除删除和插入类似，但是删除的简单之处在于只要在该节点的高度位置找到该节点，就能通过向下指针直接删除跳表中的所有相同元素。 查找SkipList的查找很简单，采用自顶向下的查找方法。从最高层开始，假设要查找的数据是k, 找到链表中的第一个位置n, 使得n &lt;= k &lt; n+1, 如果k等于n，则直接使用向下指针一路索引到最底层即可；否则n &lt; k, k介于n和n+1在下一层的节点之间。通过向下指针到达下一层继续开始查找，直到最后到达最底层为止。如下图所示： 参考文献[1] http://ticki.github.io/blog/skip-lists-done-right/ [2] https://www.jianshu.com/p/fcd18946994e [3] https://segmentfault.com/a/1190000018452079","link":"/2020/09/17/SkipList%E7%AE%80%E4%BB%8B/"},{"title":"三十天自制操作系统day3-进入32位模式并导入C语言","text":"摘要本文主要介绍《30天自制操作系统》一书第三天的内容。包括：如何使用引导扇区进行操作系统的加载以及导入C语言。从内容上看，第三天的内容可以大致分为两部分，第一部分是修改ipl.nas, 使其能够满足加载指定数目的磁盘内容(最后实现上是完成了10个扇面的加载)；第二部分是完成了一个简单的C语言编写的“操作系统”，其功能是实现不断的空转。 IPL加载磁盘内容第二天完成的ipl.nas程序，虽然名为ipl(init program loader)，但是并没有真正加载任何程序。今天我们就会解决这个问题，修改ipl.nas, 让它能真正读取磁盘内容并装载到内存中。 书中关于加载磁盘这部分，使用了4个样例项目，分别是harib00a到harib00d, 它们的区别如下： 1. harib00a: 使用简单的BIOS中断读取一个扇区的磁盘内容 2. harib00b: 在harib00a的基础上，增加了重读5次的功能 3. harib00c: 一次性读取18个扇区的内容 4. harib00d: 一次性读取10个扇面的内容 依次将这些内容呈现出来，会使得本文篇幅过长，所以这里仅仅只介绍harib00a和harib00d的实现，其中harib00d包含了中间两个项目的内容。 harib00a: 简单读取一个扇区ipl.nas最终实现1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465666768697071727374757677; hello-os; TAB=4 ORG 0x7c00 ;程序加载地址;FAT12格式 JMP entry ;跳转到入口 DB 0x90 DB &quot;HELLOIPL&quot; ;启动区的名字 DW 512 ;每个扇区的大小 DB 1 DW 1 ; FAT start positon(usually the first sector) DB 2 ; number of FAT(certainly 2) DW 224 ; capacity of root director DW 2880 ; capacity of this disk(must be 2880 sectors, totally 1440KB,1.4MB) DB 0xf0 ; class of this disk DW 9 ; length of FAT(must be 9) DW 18 ; number of sectors for each track(磁道) DW 2 ; number of header(磁头) DD 0 ; don't use fragment(must be 0) DD 2880 ; capacity for rewriting DB 0,0,0x29 ; non-sense DD 0xffffffff ; unknown DB &quot;HELLO-OS &quot; ; name for disk(11 bytes) DB &quot;FAT12 &quot; ; name of disk format RESB 18;程序体entry: MOV AX, 0 MOV SS, AX MOV SP, 0x7c00 MOV DS, AX ; 设定段寄存器 ; 读取任何内存地址都必须使用段寄存器，计算方法为： ; [DS:BX]=DS×16+BX, 通常省略[DS],写作[BX],所以必须提前设定DS为0; 从磁盘读取数据 MOV AX, 0x0820 MOV ES, AX MOV CH, 0 ; 柱面0 MOV DH, 0 ; 磁头0 MOV CL, 2 ; 扇区2 MOV AH, 0x02 ; AH=0x02 : 表示读盘 MOV AL, 1 ; 读取一个扇区 MOV BX, 0 ;[ES:BX]够成了磁盘加载到的内存地址，此处为0x0820*16+BX=0x8200 MOV DL, 0x00 ; A驱动器 INT 0x13 ;调用BIOS JC error ;如果读取失败，FLAGS.CF位为1，使用JC指令，表示：如果 ;进位为1，则出错，因此跳转到error;死循环fin: HLT JMP finerror: MOV SI, msg ;put error message;循环，通过循环不断输出结果putloop: MOV AL, [SI] ADD SI, 1 CMP AL, 0 JE fin MOV AH, 0x0e MOV BX, 0x0f INT 0x10 JMP putloop msg: DB 0x0a, 0x0a ; 2&quot;\\n&quot; DB &quot;load error&quot; DB 0x0a ; &quot;\\n&quot; DB 0 RESB 0x7dfe-$ ; 从1dfe变为7dfe, 是因为程序加载到了0x7c00 DB 0x55, 0xaa 其中大部分内容都和昨天的ipl.nas没有什么区别，区别在于我们从entry进入后，不是直接打印信息，而是装载磁盘了。当我们设定好磁盘BIOS的各种参数之后，使用一个INT 0x13指令，调用磁盘读取例程，就会将内容磁盘指定位置的内容(由扇面，磁头，扇区决定)装载到指定的内存位置(由[ES:BX]寄存器的值决定)。如果失败，FLAGS.CF位会被置1，使用JC指令，表示，一旦有进位(即FLAGS.CF位为1)，就跳转到错误位置，打印报错信息。 磁盘读取相关BIOS关于如何读写磁盘，下列网站有详细的规定：https://stanislavs.org/helppc/int_13.html。这里截下来一小部分简要说明以下： 可以看出，这里有多种磁盘BIOS例程，但它们都使用INT 0x13指令来触发，通过设置一些寄存器不同的值来区分这些不同的例程。在今天的内容中，暂时只会用到 INT 13,2 和 INT 13, 0，即读取磁盘和重置磁盘状态。关于其详细解读，如下(以 INT 13,2 为例)： 这里显示了如果我们需要调用读取磁盘例程时要设置的寄存器的值： ​ AH = 02 : 表示是读磁盘操作，顺带一提，写磁盘是0x03, 校验是0x04, 寻道是0x0c ​ AL: 要读取的扇区的数目，只能同时处理连续的扇区 ​ CH: 柱面号 ​ CL: 扇区号 ​ DH: 磁头号 ​ DL: 磁盘驱动器号 ​ [ES:BX]: 两个寄存器共同组成了加载到的内存地址。 在了解上述规定之后，就不难理解ipl.nas中的写法了。我们开始分别将0,0,2写入CH, DH, CL寄存器的目的就是分别将读取的柱面，磁头，扇区数设置为2，告诉BIOS，我们将从第0柱面的第二个扇区读取，读取的磁头号是0。这样设置的原因在于，启动扇区位于磁盘的第一个扇区，后面紧接着的就是操作系统内核，注意这里的扇区数是设置为2，而不是1，是因为扇区数是从1开始计数，而不是像通常的0那样开始计数的。关于柱面，磁头，扇区是什么，只要对存储系统有所了解应该都不陌生，这里就不详细介绍了，网上有大量关于这方面的介绍。 接下来我们使用MOV AH, 0x02将AH寄存器设置为2，表示是要读取磁盘。将AL设置为1，表示读取一个扇区。设置DL为0，表示调用A驱动器。 值得注意的是[ES: BX]寄存器的含义，在 INT 13,2 例程中，它表示装载的目的内存地址， 那为什么不直接使用一个寄存器的值作为内存地址呢？原因在于一个寄存器只有16位，至多能索引64K的地址，使用一个寄存器对表示地址，能提升地址空间范围。但是这里的地址计算并不是指将两个寄存器的值拼接在一起就表示一个地址，正确的地址计算公式如下： \\mathrm{memaddr=content(ES)\\times 16+content(BX)}就是说，是将ES寄存器中的值左移4位，再加上BX中的值，这样寻址范围可扩大到1MB。这样，我们就不难理解代码中的设置的含义了。先使用MOV AX,0x0820和 MOV ES, AX将 EX的值设置为0x0820, 后面又将BX的值设置为0， 这就表明了目的加载地址是0x8200。 最后我们要谈论的是JC error这条指令，JC的意思是Jump if Carry，即如果有进位就跳转。这里的进位是指FLAGS寄存器的CF表示位，正如说明中提到的那样，CF为1表示读取失败，JC就会发生跳转，到error的位置，进行错误信息的打印。不过正常的处理应当是一次读取失败后继续尝试，当指定次数重试仍失败时才打印报错信息，这也是我们接下来要解决的问题。 harib00d: 连续读取10个柱面的内容ipl10.nas新增内容1234567891011121314151617181920212223242526272829303132333435CYLS EQU 10...readloop: MOV SI, 0 ; 统计出错次数retry: MOV AH, 0x02 ; AH=0x02 : 表示读盘 MOV AL, 1 ; 读取一个扇区 MOV BX, 0 ; [ES:BX]够成了磁盘加载到的内存地址，此处为 0x0820*16+BX=0x8200 MOV DL, 0x00 ; A驱动器 INT 0x13 ; 调用BIOS JNC next ; 如果没有失败，即FLAGS.CF位为0，则进入fin ADD SI, 1 ; 读取失败，次数加1 CMP SI, 5 ; 和5比较 JAE error ; 如果失败次数大于等于5，则打印报错信息 MOV AH, 0x00 ; 接下来的工作是重新设置这些寄存器，对软盘进行复位 MOV DL, 0x00 INT 0x13 JMP retrynext: MOV AX, ES ; AX中现在是ES了 ADD AX, 0x0020 ; 给AX加0x0020再将AX的值写回ES相当于将内存地址后推了 0x0200 MOV ES, AX ADD CL, 1 ; 给扇区加一 CMP CL, 18 ; 和18比较 JBE readloop ; 如果扇区数小于等于18，则继续读取，也就是说， ; 如果下一个要读取的扇区数为18，则仍然要读取 MOV CL, 1 ; 又从第一个扇区开始读 ADD DH, 1 ; 磁头加1 CMP DH, 2 JB readloop ; 如果DH &lt; 2, 则继续正常读取, 否则就该增加扇面了 MOV DH, 0 ADD CH, 1 ; 扇面加1 CMP CH, CYLS ; 和10比较 JB readloop ; JB : jmp if below, 如果小于则跳转 上述内容可以直接增加到entry 设定CH,DH,CL等寄存器之后的部分。增加的部分有3个标签：readloop, retry和next，我们分别对这三个部分进行讲解。 readloop和retryreadloop包含了一个完整的读取一个扇区的内容的代码，内容包括第一句MOV SI, 0和标签retry的所有语句。这里简要说明一下：在上文中我们已经提到，有的时候读取磁盘会失败，通常我们的处理就是再试一次，如果重试次数超过5，就放弃并打印报错信息。我们用SI寄存器来放置重试的次数，每当我们读取一个新的扇区时，就将SI设置为0，然后进行接下来的操作。具体读取和重试的代码在retry中。 在retry中，我们先设定了寄存器AH,AL,BX,DL的值，然后通过INT 0x13指令调用BIOS例程。使用JNC next查看FLAGS.CF位，如果成功读取，就跳转到next, 为读取下一个扇区做准备，否则读取该扇区失败，顺着控制流向下，我们递增SI的值，表示重试次数增加了，并与5比较，如果SI的值大于等于5(说明至少5次重试了)，就跳转到error位置打印报错信息，否则继续向下进行下一次的重试。 在进行下一次重试之前，必须先恢复磁盘驱动的状态(例如磁头的位置等)，这一过程通过调用BIOS的 INT 13,0 来实现，因此我们需要设置AH,DL寄存器的值为0，然后使用INT 0x13来调用例程。最后使用一条JMP retry语句跳转到开头。 nextnext块的作用是为读取下一个块做准备工作，包括设置柱面，扇区数，以及磁头等。在讲解代码之前，我们先梳理一下这段代码的读取逻辑，在读取十个柱面时，我们是一个柱面一个柱面的读取，读取每个柱面时，又是分磁头为0和磁头为1来读取。根据书中叙述的，一个磁盘有80个柱面，2个磁头，每个柱面18个扇区，每个扇区512B。因此大体上，我们读取时，先从第一扇区读到第十八扇区，然后切换磁头，再从第一扇区读到第十八扇区，然后切换柱面，重复上述步骤。 在next中，我们还需要对内存加载地址进行修改，由于每次都是读取一个扇区，512B，即0x0200大小的内容，因此[ES:BX]也应该增加这么多，不过介于该内存地址的计算方法是ES×16+BX，所以每次只需ES的值加0x0020。这里由于不能直接操作ES寄存器，所以要用AX做一下跳板。 接下来的逻辑就比较简单了，控制流到达next,说明上一个扇区已经读取成功了，增加CL的值，指明下一个要读取的扇区数，并与18比较，如果小于等于18，即这个柱面的“这一面”还没读完，那说明还可以继续用当前这个磁头读取，就直接跳转到readloop, 否则我们需要修改DH寄存器的值表示读取该柱面的另一面，同时不要忘记重置扇区数为1(MOV CL, 1) 给DH加1后，值可能变为2,这说明该柱面读取完成了，接下来，递增CH的值表示柱面数，并与设定的CYLS比较，看看是否达到了指定读取的柱面数，如果达到了，就会向下流入fin, 否则就设定磁头为0号，继续读取(扇区数之前就已经设定好了)。 值得注意的是，这里有一条新指令EQU, 它将一个标识符设置为一个定值，如上面的CYLS。 进入32位模式并导入C语言说实话，我并不明白作者所谓的进入32位模式是什么意思，在整个代码中也没见到有明显的特权指令使得CPU从16位模式进入32位模式，这里就暂时不深究了。我就权当以为32位模式就是可以使用EAX,EBX等32位寄存器的模式。 这一部分的重点是：加载操作系统。在前面，我们已经完成了ipl程序，但它加载的磁盘并没有什么内容，而且在完成加载后只是一直空转，没有执行别的东西，这一部分，我们将完成一个简单的操作系统，并让它真正运行起来。 明确操作系统加载的位置为了使操作系统能真正地运行起来，我们必须知道操作系统内容被加载到了哪个内存位置，之后在ipl中直接使用一条JMP跳转到指定内存位置即可。 在前面，我们说过，加载磁盘内容时，第二个扇区对齐内存0x8200的位置，后面的扇区顺着放置即可。因此，只需要知道操作系统在磁盘中的位置即可。 为此，我们先写一个简单的操作系统内核 haribote.nas: 123fin: HLT JMP fin 然后将该操作系统编译后形成的bin文件和ipl文件放置在一起，这部分的内容是在Makefile中体现的： 12345678haribote.sys: haribote.nas Makefile $(NASK) haribote.nas haribote.sys haribote.lstharibote.img: ipl.bin haribote.sys Makefile $(EDIMG) imgin:../../tolset/z_tools/fdimg0at.tek\\ wbinimg src:ipl.bin len:512 from:0 to:0\\ copy from:haribote.sys to:@:\\ imgout:haribote.img 从上面的Makefile可以看出，我们先使用nask.exe将haribote.nas编译为haribote.sys, 然后将其拼接为haribote.img文件。通过hexdump工具，我们观察一下这个haribote.sys到底位于haribote.img中哪个位置： 下图为haribote.sys的内容： 下图为haribote.img的内容 通过比较即可发现，操作系统的内容被放置在了磁盘中0x4200的位置，所以加载时的实际内存地址应该为: 0x4200 - 0x0200 + 0x8200=0xc200。 为此，我们将在完成加载之后使用JMP 0xc200的指令跳转到该内存地址，这部分在ipl.nas中修改。将原先next标签后面加上JMP 0xc200即可。 导入C语言今天的内容关于读取磁盘和加载操作系统的部分已经讲解的差不多了，在原书中，作者还添加了一节修改haribote.nas文件，大概修改的内容就是设定画面显示模式，这个我们不需要具体说了。接下来我们将使用C程序代替我们的操作系统内核并完成加载。 事实上，作者将haribote.nas改写为了asmhead.nas并添加了一百多行的代码用于最后启动C程序中的函数，这部分我也没理解透，这里就不出来丢人了。这部分简单说一说最后的C程序： bootpack.c1234567void io_hlt(void);void HariMain(void){ fin: io_hlt(); goto fin;} 这里的C程序很简单，就是空转。但是io_hlt是如何调用的呢？这里只有io_hlt的声明但是没有定义，事实上，定义在naskfunc.nas中，该文件中所有代码如下： 12345678910111213141516171819; naskfunc; TAB=4[FORMAT &quot;WCOFF&quot;] ;设定目标文件模式[BITS 32] ; 值作32位模式使用的机器语言; 值作目标文件的信息[FILE &quot;naskfunc.nas&quot;] ;源文件名称 GLOBAL _io_hlt ;程序中包含的函数名;实际的函数[SECTION .text]_io_hlt: ;实际上就是void io_hlt() HLT RET 在这里，我们使用GLOBAL修饰了_io_hlt, 表明这个函数可以在其他文件使用(正如C的修饰符extern一样)，如果我们想在链接时，C程序能正确找到这个函数的实现，则函数名必须一致，且在函数名之前加上一个下划线。 [SECTION .text]下面是函数的实现，使用_io_hlt:指明是哪个函数的实现，这里的HLT较为熟悉，RET指令的目的类似于return语句，用于返回控制。 C程序编译过程因为这本书使用的是作者设计的编译器，所以一切都要按照他的想法来。在这里，想要将C程序转为机器代码并写入磁盘，还有一系列的步骤： cc1.exe : bootpack.c -&gt; bootpack.gas, 将C程序编译为gas汇编语言 gas2nask.exe: bootpack.gas -&gt; bootpack.nas, 这一步是将gas汇编语言转为我们一直使用的nas汇编语言 nask.exe: bootpack.nas -&gt; bootpack.obj, 这一步就是我们一直使用的编译过程，将nas汇编转为机器指令，但是这里的机器指令还未经过链接和重定向等处理。 obi2bim.exe: 多个obj文件 -&gt; bootpack.bim，这一步完成的是链接工作，将多个obj文件链接成为一个完整的bim文件(binary image file), 这里的bim文件是作者自己设计的一种格式 bim2hrb.exe: bootpack.bim -&gt; bootpack.hrb, 将上一步得到的bim文件转换为hrb文件，这里作者也没有提供过多的解释，可以理解为对文件格式进行了改进，增加了文件头等内容。 完成bootpack.hrb之后，就可以将bootpack.hrb与asmhead.bin放置在一起，形成haribote.sys文件了。这就是初步的操作系统内核文件，而要得到镜像文件，还需要ipl.bin。 参考资料《30天自制操作系统》","link":"/2020/07/10/%E4%B8%89%E5%8D%81%E5%A4%A9%E8%87%AA%E5%88%B6%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9Fday3-%E8%BF%9B%E5%85%A532%E4%BD%8D%E6%A8%A1%E5%BC%8F%E5%B9%B6%E5%AF%BC%E5%85%A5C%E8%AF%AD%E8%A8%80/"},{"title":"三十天自制操作系统day2:汇编语言学习与Makefile入门","text":"摘要本文主要介绍《30天自制操作系统》一书第二天的内容。内容包括：使用作者设计的nas汇编语言编写简单的IPL(Init Program Loader), 该语言在编译后与第一天手动写入的二进制文件相同；学习简单的nas汇编语言；制作简单的Makefile文件。在本文末尾会简要介绍如何使用Virtual-Box装载并调试有img镜像文件的虚拟机，该部分内容来源于B站up主：技术宅阿棍，视频网址为：https://www.bilibili.com/video/BV1dx411d7DL?from=search&amp;seid=17279356591739783403 nas汇编与IPL程序nas汇编简介nas汇编是作者自己设计的汇编语言，该汇编语言与x86汇编极为相似，想完成本书涉及的实验也必须使用nas汇编和作者自己写的汇编器将汇编代码转换为机器代码。这里仅仅简单介绍在第二天的实验中会用到的一些寄存器和指令。 nas寄存器nas寄存器和x86汇编寄存器保持一致，主要有以下几个： 1234567891011121314AX //accumulator, 累计寄存器CX //counter, 计数寄存器DX //data, 数据寄存器BX //base, 基址寄存器SP //stack point, 栈指针寄存器BP //base pointer, 基址指针寄存器SI //source index, 源变址寄存器DI //destination index, 目的变址寄存器ES //附加段寄存器CS //代码段寄存器SS //栈段寄存器DS //数据段寄存器FS //没有名称(segment part2)GS //没有名称(segment part3) 上述寄存器均为16位寄存器，除此之外还有诸如AL,AH,BL,BH等寄存器，实际上它们表示的不是新的寄存器，而是对原16位寄存器的高低8位的使用，比如AL寄存器表示对AX寄存器低8位的使用，AH寄存器表示对AX寄存器高8位的使用，可以理解为L和H分别代表Low和High。 作者这里也提到了一个有趣的事实，就是各个寄存器的命名，根据英文和中文注释可以看出，各个寄存器命名都是来源于其英文含义的单词首字母，AX,BX,CX,DX是特例，它们的X来源于extend中的x, 进一步地，32位寄存器中的eax, ebx, ecx,edx中的e也来源于extend 熟悉x86汇编的人对上述寄存器的使用方式应该并不陌生。我们可以将上面的寄存器分为两类，以ES为分界线，ES以上的8个寄存器用于普通的运算和寻址，ES包括自己以下的寄存器为段寄存器，用于对内存进行分段管理。而上面的8个寄存器又可以分为两类，前4个为数据寄存器，后四个为指针寄存器和变址寄存器。 对于每个寄存器的详细使用，这里并不做过多介绍，实际上在阅读源代码的过程中自然就能体会到每个寄存器的使用方式。简单地说： AX,BX,CX,DX用于一般的算术计算，如加减乘除以及数据传输等。还包括一些寻址方式，比如BX的作用是存放基地址，因此它可以用于形成地址索引 SP,BP,SI,DI 是指针寄存器和变址寄存器，SP指向当前的栈顶，BP是基址指针寄存器，可以用于形成相对于SS的一个基址位置，SI用于存放相对于ES段的源变址指针，DI则是存放相对于ES段的目的变址指针 nas指令这里讲解一些day2实验中要用到的nas指令，它们基本上与x86汇编保持一致，这里的形式可能不太规范，原因是下列指令的总结仅来源于day2的代码，还不清楚每条指令的使用方法。请各位谅解 1234567MOV r1,r2 //在寄存器r1和r2之间传递数据，方向是从r2到r1，其中r1也可以是立即数 ,源数据与目的数据位数必须相同ADD r1,1 //给r1寄存器加1，目前还不清楚该ADD指令是否支持像x86那样的三地址形式 ,还是仅仅支持累加形式CMP r1,r2 //比较两个寄存器的值是否相等，并且设置标志位寄存器，这里r2也可以是立即数JE s //查看标志位寄存器的值，如果比较相等，则跳转到标号s所指的位置INT s //中断处理，s是中断号JMP s //无条件跳转到标号s的位置HLT //使CPU暂时休眠，等待指令 此外，作者还使用了一条ORG指令，该指令指定程序的装载位置，但严格来说，我并不认为这是一条指令，而更像是一条指导语句。 寻址模式nas提供的寻址模式仍与x86类似，我们可以使用一个立即数来索引一个内存地址，也可以使用寄存器中的值来索引一个内存地址，但不管哪种方式，都必须使用中括号[ ]表示这是一个内存引用。 nas寻址的一般形式为 : 数据大小 [地址] 其中数据大小有:BYTE, WORD, DWORD, 分别表示字节，字，双字，其大小分别为1,2,4个字节。下面是一些例子： 12MOV AL, BYTE [SI] //将SI中的值作为内存地址，将该地址处的一个字节读取到AL中MOV AL, [SI] //由于MOV指令要求源和目的数的位数必须相同，AL为8位的，所以省略BYTE结果仍是读取一个字节 IPL程序下面将展示今天的最终成果，IPL程序，该程序虽然被称为初始程序加载器，但它并没有加载任何程序，而只是打印了一些字符串而已。该程序的主要流程是：设定FAT12格式，跳转并初始化寄存器，然后开始打印输出，在打印时需要我们先设定打印值以及颜色等参数，然后通过中断调用显卡的BIOS打印。 源程序 ipl.nas123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960; hello-os; TAB=4 ORG 0x7c00 ;程序加载地址;FAT12格式 JMP entry ;跳转到入口 DB 0x90 DB &quot;HELLOIPL&quot; ;启动区的名字 DW 512 ;每个扇区的大小 DB 1 DW 1 ; FAT start positon(usually the first sector) DB 2 ; number of FAT(certainly 2) DW 224 ; capacity of root director DW 2880 ; capacity of this disk(must be 2880 sectors, totally 1440KB,1.4MB) DB 0xf0 ; class of this disk DW 9 ; length of FAT(must be 9) DW 18 ; number of sectors for each track(磁道) DW 2 ; number of header(磁头) DD 0 ; don't use fragment(must be 0) DD 2880 ; capacity for rewriting DB 0,0,0x29 ; non-sense DD 0xffffffff ; unknown DB &quot;HELLO-OS &quot; ; name for disk(11 bytes) DB &quot;FAT12 &quot; ; name of disk format RESB 18;程序体entry: MOV AX, 0 MOV SS, AX MOV SP, 0x7c00 MOV DS, AX MOV ES, AX MOV SI, msg;循环，通过循环不断输出结果putloop: MOV AL, [SI] ADD SI, 1 CMP AL, 0 JE fin MOV AH, 0x0e MOV BX, 0x0f INT 0x10 JMP putloop ;死循环fin: HLT ;HLT的作用是使CPU休眠，这样做可以减小消耗 JMP finmsg: DB 0x0a, 0x0a ; 2&quot;\\n&quot; DB &quot;Operating System, by kqh&quot; DB 0x0a ; &quot;\\n&quot; DB 0 RESB 0x7dfe-$ ; 从1dfe变为7dfe, 是因为程序加载到了0x7c00 DB 0x55, 0xaa 源码分析：上述代码中的第一句ORG 0x7c00， 前面已经说过，是将程序装载到0x7c00这个内存位置，至于为什么0x7c00, 这些应该是在当今的体系结构下规定好的。紧接着，我们使用一条JMP entry指令，跳转到了entry入口处，跟随在JMP指令之后的是一系列FAT12文件系统的参数设定。 在entry之后的一系列代码包括以下几个内容： 初始化寄存器123456MOV AX, 0MOV SS, AXMOV SP, 0x7c00MOV DS, AXMOV ES, AXMOV SI, msg 这段代码，最终的结果就是将SP寄存器的值设置为0x7c00, 将SI的值设置为msg这个标号的位置，msg表示了将要输出的结果。除此之外，其他寄存器的值均为0 putloop循环123456789putloop: MOV AL, [SI] ADD SI, 1 CMP AL, 0 JE fin MOV AH, 0x0e MOV BX, 0x0f INT 0x10 JMP putloop 该循环的主要作用是输出msg标号中保存的数据。至于数据的具体内容，待会再看，我们只需知道该数据的内容就是一个一个连续的字节，以0作为结尾，开始内存位置现在已经存于SI寄存器中了，我们每进入一次循环，先将当前的SI内存地址中的数据加载到AL中，然后SI加一，指向下一个内存地址。比较AL中的值是否为0，如果是，说明已经输出到末尾了，就跳转到fin标号的位置，表示结束。如果判断条件不成立，还会继续执行，继续执行的语句是两条MOV和一条INT，我们先说说INT是什么意思 INT表示中断，这里的意思是，我们需要调用外部的一些设备来处理视频输出的问题。我们可以将其暂时视作一个函数调用，而0x10就是区分不同的调用，在这里，0x10的意思就是我们要输出到屏幕上。关于中断的详细介绍，作者在这里(P38)提供了一个网站:http://community.osdev.info/?(AT)BIOS, 不过好像已经失效了，这里提供另外一个网址：http://www.bioscentral.com/misc/biosservices.htm 在该网址中，可以查询到0x10明确地表示了中断服务：Video service routine，如下图所示： 在下一个网站:https://stanislavs.org/helppc/int_10.html我们能够查到INT 0x10中断触发的不同类型，这里的类型是由AH寄存器的值来决定的： 由于我们的AH寄存器的值为0x0e, 所以我们查看INT 10,E, 结果如下： 这里规定了AH寄存器的值必须为0x0e, 它规定了触发0x10的中断类别，AL寄存器的值表示了将要输出的ASCII码， 这里就是将SI寄存器的值装载到AL中。 BH和BL分别表示了输出的页码和颜色。我们将BX整个设置为0x0f的目的就是将BH设置为0，表示页码为0，BL设置为0x0f,表示输出颜色，而颜色作为一个8bit的数据，高4位表示输出的背景颜色，这里为0，表示黑色；低四位表示输出的字符颜色，这里为0xf, 表示白色。也就是说，会输出黑底白字的数据。 fin这是一个死循环，每当进入fin就会在短暂地休眠之后又JMP到fin。HLT的目的是使得CPU进入休眠状态，这样能够使CPU的功耗减少 msg12345678msg: DB 0x0a, 0x0a ; 2&quot;\\n&quot; DB &quot;Operating System, by kqh&quot; DB 0x0a ; &quot;\\n&quot; DB 0 RESB 0x7dfe-$ ; 从1dfe变为7dfe, 是因为程序加载到了0x7c00 DB 0x55, 0xaa 该标号表示了要输出的内容是什么。上面的内容上看，我们依次写入了两个0x0a, 以及一个字符串内容&quot;Operating System, by kqh&quot;, 然后又是一个换行符0x0a， 最后写入一个0，写入零的目的是区分一个字符串的结束。然后我们使用了一条语句RESB 0x7dfe-$, 这里的RESB的意思是reset byte, 就是将从目前位置开始一定数量的字节清零，那么清零的字节数是多少呢？就是0x7dfe-$。 这里必须提到`的作用，`记录了当前已写入的字节数，但由于我们开始使用ORG语句将程序加载到了0x7c00的位置，所以会加上一个0x7c00的基地址(暂且这样理解吧)。我们制作的IPL程序仅仅只占用第一个扇区，即前512个字节，所以就是说从0x7e00开始就是第二个扇区了(如果顺序排放的话)。而且，规定了在第一个扇区的最后两个byte处放置0x55和0xaa, 所以我们要填满0x7dfe-$这么多的数据。 运行情况我们使用asm.bat批处理程序来编译ipl.nas程序，得到ipl.bin，再使用ipl.bin生成helloos.img得到最后的磁盘文件。 我们使用VirtualBox加载生成的img文件，得到的运行结果如下： Makefile入门编译工作makefile的结构大致可以归纳为如下形式： 12target : dependences operation 其中target是指我们想要编译得到的目标程序，dependences是指得到该程序需要的依赖，包括源文件，obj文件等。operation是指我们的具体操作，比如调用编译器等等。 这里以ipl.bin的编译过程为例： 12ipl.bin : ipl.nas Makefile ../../tolset/z_tools/nask.exe ipl.nas ipl.bin ipl.lst 上述语句是说，我们想到得到ipl.bin, 就必须先有ipl.nas和Makefile文件。一旦检查有这两个文件，就会启动../../tolset/z_tools/nask.exe, 该编译器接受源程序ipl.nas, 编译生成ipl.bin和ipl.lst文件。 当我们想要使用该makefile程序编译得到ipl.bin时，只需要在命令行中输入make ipl.bin即可。 其他工作run: 1234run: ../../tolset/z_tools/make.exe img copy helloos.img ..\\..\\tolset\\z_tools\\qemu\\fdimage0.bin ../../tolset/z_tools/make.exe -C ../../tolset/z_tools/qemu clean: 123clean: #删除目标程序 -del ipl.bin -del ipl.lst src_only: 123src_only: #删除除了源代码以外的所有文件 ../../tolset/z_tools/make.exe clean -del helloos.img 附录Virtual-Box装载自己的img文件使用Virtual-Box装载自己的img文件需要先准备好vbox和img文件。然后按照以下步骤执行： 创建虚拟机点击vbox中的新建，然后选择对应的操作系统和型号，由于我们是从自己的img文件启动，所以这里选择什么操作系统都无所谓： 然后一直点击下一步，按照它默认设置即可，由于这本书最后制作出来的操作系统本身也不大，占用内存也不多，所以内存设置和硬盘设置也不需要太大。在创建虚拟硬盘的地方选择固定大小,最后点击创建 到目前为止，我们已经创建完成了一个新的虚拟机了，但是由于没有加载操作系统，所以还不能真正运行。 设置从软驱启动对刚刚创建好的这个虚拟机，点击设置选项： 点击其中的存储设置： 在图中所示的位置，有一个添加控制器选项，点击后选择添加软盘控制器。然后上方在存储介质一栏就会出现 控制器：Floppy的字样了。点击右侧的添加虚拟软驱，如下图所示： 然后浏览选择自己要加载的img文件即可。 然后点击OK并运行该虚拟机即可。 Virtual-Box调试在Virtual-Box所在文件夹打开git bash, 然后输入以下两条命令 12export VBOX_GUI_DBG_AUTO_SHOW=trueexport VBOX_GUI_DBG_ENABLED=true 再输入./VirtualBox运行并打开虚拟机即可 参考资料《30天自制操作系统》","link":"/2020/06/20/%E4%B8%89%E5%8D%81%E5%A4%A9%E8%87%AA%E5%88%B6%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9Fday2-%E6%B1%87%E7%BC%96%E8%AF%AD%E8%A8%80%E5%AD%A6%E4%B9%A0%E4%B8%8EMakefile%E5%85%A5%E9%97%A8/"},{"title":"三十天自制操作系统day4,day5-画面显示","text":"摘要本文主要介绍《30天自制操作系统》一书第4，5天的内容。由于这两天的内容比较类似而且内容上极度相关，所以我决定将这两部分的内容一起编写，而且在该系列以后的文章中也会采用将内容关联紧密的部分一起编写的形式。这两天的内容主题就是画面显示， 顺序是从最开始的显示条纹，到显示操作系统图形界面，再到打印字符串和光标，最终完成了一个图形界面的操作系统雏形。 图形显示图像显示的基本原理要制作图形界面的操作系统，就要明白操作系统是如何在屏幕上显示图像的。在第三天的内容中，我们通过haribote.nas中的一段内容开启了320×200的分辨率模式(后来迁移到asmhead.nas中)，并且在该模式下，显存的地址为0xa0000。实际上，简单地说，我们可以将屏幕上的每个像素点理解为一个小块，每个小块只能显示一种颜色，当我们为320×200个小块每个都指定一种颜色时，就能在屏幕上看到颜色了。如果再加上一些位置的信息，就能显示图形了。 组织结构为每个小块指定颜色是怎么做到的呢？其实就是向内存中指定位置写数。每个像素点占用一个字节的数据。在我们的分辨率320×200分辨率，一共就需要占用320×200个字节的内存。这些连续内存的起始位置就是0xa0000，且按照行存储，意思是第一行320个像素点，占据从0xa0000开始的320个连续字节，然后是第二行的320个像素点，依次类推。如下图所示： 该模式下实际的现存空间大小是0xa0000~0xaffff,但这里只占用了一部分。 调色板上面的解释基本说明了屏幕显示图像的原理，不过还有一个问题。我们都知道图像的显示是采用RGB三通道显示的，每个通道都是一个0~255的数值，占用一个字节。按理说我们想要使屏幕显示正常的图像，需要三通道三个值，也就是3个字节才对，但这样的话，显然显存大小就不能满足我们的需求了。解决这一问题的方法就是调色板，通过将0~255的数字映射到任意一种颜色的RGB表示，我们就只需要在VRAM(指显存，下同)中写入对应颜色的色号即可，而写入色号只需1个字节，满足上面的要求。这种映射的方法被称为调色板 关于调色板的设定，书中给出了一个函数init_palette，该函数本质上就是将我们需要的颜色的RGB三通道表示出来然后写到对应的位置。如下所示： 123456789101112131415161718192021222324void init_palette(void){ static unsigned char table_rgb[16 * 3] = { 0x00, 0x00, 0x00, /* 0: 黑 */ 0xff, 0x00, 0x00, /* 1: 亮红 */ 0xff, 0xff, 0x00, /* 2: 亮绿 */ 0xff, 0xff, 0x00, /* 3: 亮黄 */ 0x00, 0x00, 0xff, /* 4: 亮蓝 */ 0xff, 0x00, 0xff, /* 5: 亮紫 */ 0x00, 0xff, 0xff, /* 6: 浅亮蓝 */ 0xff, 0xff, 0xff, /* 7: 白 */ 0xc6, 0xc6, 0xc6, /* 8: 亮灰 */ 0x84, 0x00, 0x00, /* 9: 暗红 */ 0x00, 0x84, 0x00, /* 10: 暗绿 */ 0x84, 0x84, 0x00, /* 11: 暗黄 */ 0x00, 0x00, 0x84, /* 12: 暗青 */ 0x84, 0x00, 0x84, /* 13: 暗紫 */ 0x00, 0x84, 0x84, /* 14: 浅暗蓝 */ 0x84, 0x84, 0x84 /* 15: 暗灰 */ }; set_palette(0, 15, table_rgb); /* 写入16中颜色，从0到15 */ return;} 上述代码中每一行表示一个颜色的RGB三通道分量。 接下来就需要将上述RGB分量设置到调色板了。这部分内容涉及到IO操作，简单来说，IO操作并不属于内存操作(x86)，因此需要使用特殊的IO指令IN和OUT, 指令的格式是通过OUT可以将指定数据写到指定的“位置”，称为端口，或通过IN从指定的端口读取数据。这部分在书中有一定的描述，但是作者给出的参考网址已经失效了，这里给出另外两个参考网址，第一个是关于x86端口的对照表和简单说明，第二个是关于VGA图像设定的说明，包括如何设定调色板，如何向显存中写入数据来显示等等，还包括了对应的C语言实现。 http://bochs.sourceforge.net/techspec/PORTS.LST http://www.delorie.com/djgpp/doc/ug/graphics/vga 通过上述参考资料。我们不难明白，设定一个调色板，我们需要先向0x03c8端口写入该颜色的色号，然后向0x03c9中分别写入R,G,B三通道的分量。其中向0x03c8写入的动作表明是PEL address write mode（见上述第一个网页中关于0x03c8的解释,PEL表示palette，指调色板)。同理，我们还可以向0x03c7写入色号, 该动作表示是PEL address read mode(理由同上)，表明是读出该色号对应颜色，随后从0x03c9读取三次就能读出该颜色的RGB分量。 书中还提到，如果要连续设置调色板，只需要在第一次设置的时候填入色号即可。因此，我们不难写出下面的设置调色板程序: 1234567io_out8(0x03c8, start);for(i = start; i &lt;= end; ++i){ io_out8(0x03c9, rgb[0] / 4); io_out8(0x03c9, rgb[1] / 4); io_out8(0x03c9, rgb[2] / 4); rgb += 3;} 上述代码中，start表示初始色号，end表示终止色号。不过让我费解的是此处rgb分量都除以了4，不知道是出于什么原因。 填充颜色在完成上述调色板的设置之后，我们可以着手开始图像的绘制了。由于设置了调色板，我们可以用如0，1，2等数字表示某种特定的颜色，如0表示黑色，1表示亮红色等等。当我们向一个像素对应的显存位置写入1时，该像素点就会显示出亮红色。 位置与显存对应：为了方便我们绘制图形，我们需要对整个屏幕建立坐标系，通常来说，该坐标系的原点是屏幕的左上角的第一个像素，沿屏幕的水平方向为x轴，竖直方向为y轴，这是接下来的统一规定。如下图所示： 很明显，当我们尝试写一个位置为(x, y)的像素点时，按照上面所说的显存的组织方式，该点前面有y个完全填满的行，自己在所在行的x+1列，前面有x个像素点，因此不难得出其显存位置是: vram[y * xsize + x]。至于xsize如何获得，我们先将其值固定为320，以后会描述如何从内存中获得该值。 矩形绘制和图像界面制作有了上述关于图形绘制的铺垫。我们可以着手进行图形界面的绘制了。第一步是如何绘制矩形，因为图形界面的大部分位置都比较平整和规则，所以绘制矩形将是一种很好的抽象调用。 表示一个矩形的方法就是利用其左上角的顶点位置和右下角的顶点位置进行标识，然后用一个for循环将该范围内的所有内存赋值即可。具体代码如下： 123456789void boxfill(unsigned char *vram, int xsize, unsigned char c, int x0, int y0, int x1, int y1){ int x, y; for(y = y0; y &lt;= y1; ++y){ for(x = x0; x &lt;= x1; ++x){ vram[y * xsize + x] = c; } }} 函数的参数除了给出一个矩形的唯一标识之外，还给出了绘制图形的颜色c， 以及一行的像素数xsize。值得说明的是，尽管被称为矩形，该函数也可以绘制一条“线”的图形，只需让y0和y1相等或让x0和x1相等即可。 下一步就是制作图形界面了。这一部分主要还是将提前绘制好的图形转换成矩形的绘制，再辅助一些阴影的表示就能达到很好的效果了。代码如下： 1234567891011121314boxfill(vram, xsize, COL8_008484, 0, 0, xsize - 1, ysize - 29); boxfill(vram, xsize, COL8_C6C6C6, 0, ysize - 28, xsize - 1, ysize - 28);boxfill(vram, xsize, COL8_FFFFFF, 0, ysize - 27, xsize - 1, ysize - 27); boxfill(vram, xsize, COL8_C6C6C6, 0, ysize - 26, xsize - 1, ysize - 1);boxfill(vram, xsize, COL8_FFFFFF, 3, ysize - 24, 59, ysize - 24);boxfill(vram, xsize, COL8_FFFFFF, 2, ysize - 24, 2, ysize - 4);boxfill(vram, xsize, COL8_848484, 3, ysize - 4, 59, ysize - 4);boxfill(vram, xsize, COL8_848484, 59, ysize - 23, 59, ysize - 5);boxfill(vram, xsize, COL8_000000, 2, ysize - 3, 59, ysize - 3);boxfill(vram, xsize, COL8_000000, 60, ysize - 24, 60, ysize - 3);boxfill(vram, xsize, COL8_848484, xsize - 47, ysize - 24, xsize - 4, ysize - 24);boxfill(vram, xsize, COL8_848484, xsize - 47, ysize - 23, xsize - 47, ysize - 4);boxfill(vram, xsize, COL8_FFFFFF, xsize - 47, ysize - 3, xsize - 4, ysize - 3); boxfill(vram, xsize, COL8_FFFFFF, xsize - 3, ysize - 24, xsize - 3, ysize - 3); 运行效果如下： 到目前为止，第四天的内容算是基本上完成了。我们的操作系统已经初步具有图形界面了。接下来我们将讲解打印字符和字符串的相关内容，这为我们的调试实际上提供了很大的便利。 字符与字符串显示在上一节中，我们简单介绍了操作系统图像界面的绘制。此时的屏幕上已经有了初步的图形界面。但是还并没有能与用户交互的任何信息，属于完全静态的界面。因此我们接下来将介绍如何打印字符与字符串，用于初步的交互，以及方便打印内存内容用于调试。 显示字符的基本原理基本启动信息的获取上一节有一个遗留问题。就是关于启动信息的处理。包括显存的起始为止0xa0000以及每一行的像素数目xsize我们都是通过规定为常数的方式进行处理的。但是如果操作系统切换了显示模式，那我们又需要重新更改上述数值，更严重的是，这些数值并不是真实根据用户选择的启动模式而改变，而仅仅是因为作为设计者的我们知道这些参数的具体数值而被固定。显然不具有好的扩展性。 实际上，在上面一节的内容中，提到了在asmhead.nas的代码中早已经提前存储了启动信息，如下所示： 1234MOV BYTE [VMODE] 8MOV WORD [SCRNX] 320MOV WORD [SCRNY] 200MOV DOWRD [VRAM] 0x000a0000 因此在后续我们需要实现的就是将它们依次取出来即可。为了取出该数据，我们需要知道其占据的内存地址，该地址为0x0ff0, 为了将从0x0ff0开始的几个连续数据取出，使用结构体即可。如下： 1234567struct BOOTINFO{ char cyls, leds, vmode, reserve; short scrnx, scrny; char *vram;}struct BOOTINFO *binfo = (struct BOOTINFO *)0x0ff0; 通过binfo中的相应字段我们就能访问启动的基本信息了。上面的结构体中有一个reserve的字段，个人认为这是为了对齐而设计的，应该没有什么用。实际上在接下来的几天中我们用到的也仅仅只是vram, scrnx这两个字段。 字符的显示要理解如何显示字符，必须先明白一个字符也只是多个像素点集合而已。指定一个像素点的具体显示颜色就能得到一个字符的显示(当然也可以对一个字符用不同的颜色)。字符的显示，通常具有规则的特性，即一个字符占用的像素点的数目是固定大小的。在本实现中，我们采用的是高16，宽8的小方块作为一个字符绘制的范围。在该范围内，有一部分像素点是上色的，有一部分是不上色的。因为只有两种属性，所以我们直接使用0，1分开标识。这样，每一行实际上是长度为8的0，1串，用一个字节即可表示，16行共16个字节。也就是说我们只需要使用16个字节就能展示一个字符。 例如大写字母A的表示方法如下： 1234static char font_A[16] = { 0x00, 0x18, 0x18, 0x18, 0x18, 0x24, 0x24, 0x24, 0x24, 0x7e, 0x42, 0x42, 0x42, 0xe7, 0x00, 0x00}; 有了表示方法，正如上面所说的。我们只需要对每一行，看一看该字节中哪些位是1，然后绘制上对应的颜色即可。因此我们抽象了putfont8函数，该函数指定一个输出位置(用x,y表示)，以及要输出字符对应的16字节表示缓冲区，还有绘制的颜色，通过对vram指定位置写数据来达到输出字符的目的。 我们绘制字符时，是一行一行绘制的，因此采用一个for循环遍历16次，读取一个字节，每一次，都采用位运算遍历这个字节的每一位，如果是1，则在vram中写入对应颜色，否则不写。 这里比较关键的是坐标位置到vram的对应。假如函数要求我们在x,y为左上角的16×8方块开始写字符，那么对于第 i 行(i 从0开始), 其开始的内存地址就是char *p = vram + y * xsize + x + i * xsize, 然后我们遍历改行对应的这个字节的每一位，从第一位开始，我们需要将1左移7位，因此初始的循环变量j是7，然后依次减小。对应的在行的内存地址为p[7 - j]。综上，我们不难写出绘制字符的程序： 12345678910111213void putfont8(unsigned char *vram, int xsize, int x, int y, char c, char *font){ int i; char d; for(i = 0; i &lt; 16; ++i){ char *p = (char *)(vram + (y + i) * xsize + x); d = font[i]; int j; for(j = 7; j &gt;= 0; --j){ if(d &amp; (1 &lt;&lt; j)) p[7 - j] = c; } }} 更多字体和字符串显示更多字体上面的说明虽然阐明了如何显示一个字符，但对于多个字符，我们不可能为每个字符都手动写一个长度16的char数组。为此，原书作者提供了一个hankaku.txt文件，它采用了上面提到的16×8方块的表现形式。大致如下所示： 在上述文件中，共有256个ASCII字符，因此上述表示总共需要256×16=4096个字节的空间。原书作者提供了一个makefont.exe的程序用于将上述显示模式转换为16个字节的表示形式, 该文件最终会被转换为.obj文件用于和我们的程序进行链接。 为了使用这些字符的16字节表示形式，我们必须先在主程序中声明该内容，使用extern hankaku[4096]语句达到这一目的。然后对于特定的字符a, 其表示的首位置所在的坐标为hankaku + a * 16, 其中表达式中的a表示该字符的ASCII码。例如下面的代码绘制了字符串”ABC 123”: 123456putfont8(binfo-&gt;vram, binfo-&gt;scrnx, 8, 8, COL8_FFFFFF, hankaku + 'A' * 16);putfont8(binfo-&gt;vram, binfo-&gt;scrnx, 16, 8, COL8_FFFFFF, hankaku + 'B' * 16);putfont8(binfo-&gt;vram, binfo-&gt;scrnx, 24, 8, COL8_FFFFFF, hankaku + 'C' * 16);putfont8(binfo-&gt;vram, binfo-&gt;scrnx, 40, 8, COL8_FFFFFF, hankaku + '1' * 16);putfont8(binfo-&gt;vram, binfo-&gt;scrnx, 48, 8, COL8_FFFFFF, hankaku + '2' * 16);putfont8(binfo-&gt;vram, binfo-&gt;scrnx, 56, 8, COL8_FFFFFF, hankaku + '3' * 16); 打印字符串上述代码实现了打印字符串的方式，就是将每个字符一个一个地打印出来。但是这样做，一方面不能和C语言的字符串表示一致, 例如上面的putfont8函数并没有打印空格；另一方面写起来也非常麻烦。实际上采用一个循环就能解决这个问题： 123456789101112void putfont8_asc(unsigned char *vram, int xsize, int x, int y, char c, unsigned char *s){ extern char hankaku[4096]; for(; *s != 0x00; ++s){ putfont8(vram, xsize, x, y, c, hankaku + *s * 16); x += 8; if(x &gt; 312){ x = 0; y += 8; } }} 简单地说，对于一个字符串中的所有字符，我们依次打印它们，当一行打印不下来的时候，我们就跳到下一行(原书的作者似乎没有注意到这一点)继续打印。不过上述有一点不足就是这里的312实际上是320减8的结果，表明剩下的空间不足以再放置一个字符了。这里最好的处理是使用xsize-8来表示。 最终的运行效果如下： 书中还有一处是使用输出特定格式的字符串的。这部分使用了stdio.h头文件，然后使用了sprintf函数，这是没有问题的，但是千万不能使用printf函数，因为这里面包含了系统调用，而我们的操作系统还没有实现这一点。 到目前为止，我们已经完成了字符串的打印，图形界面的绘制等等工作了。还有一小部分内容是鼠标的绘制，但实际上和上面的方法并没有什么区别，这里就不再详细说明了。接下来我们要做的就是完成交互式操作，即让操作系统能对用户的键盘和鼠标指令发出响应，这涉及到中断处理，因此我们放到下一篇文章来说。 参考资料《三十天自制操作系统》 http://bochs.sourceforge.net/techspec/PORTS.LST http://www.delorie.com/djgpp/doc/ug/graphics/vga","link":"/2020/08/05/%E4%B8%89%E5%8D%81%E5%A4%A9%E8%87%AA%E5%88%B6%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9Fday4-day5-%E7%94%BB%E9%9D%A2%E6%98%BE%E7%A4%BA/"},{"title":"投射模和内射模","text":"前言本文简要介绍关于投射模和内射模的部分知识","link":"/2020/07/27/%E6%8A%95%E5%B0%84%E6%A8%A1%E5%92%8C%E5%86%85%E5%B0%84%E6%A8%A1/"},{"title":"我的博客搭建","text":"写在前面在2019年9月份的时候，因为闲来无事，我便第一次搭建了自己的hexo博客，虽然时间过去的已经较为久远了，但我还是依稀记得初次搭建博客的兴奋以及完成之后的满足感。不过由于大三上学期学业繁忙，尤其是操作系统和体系结构两大实验占据了我大量时间，使得我没有多余的精力去经营自己的博客，所以仅仅在留下几篇简单的博文之后便没有再过问了，后来又重新装了系统，导致本来就很难找到的blog文件夹这下是彻底不知所踪了。后来在家上网课时，因为查询资料见识了多位大佬的博文，也重新燃起了我写博客的热情，于是借着这次因为疫情而呆在家里的机会，我重新搭建了自己的博客。目前来看，尽管过程很曲折，但大致的功能已经实现了，我也觉得很有必要用一篇博文来纪念一下整个搭建过程。 博客搭建本次博客搭建的过程大部分参考自b站up主codesheep的教学视频，网站链接如下：https://www.bilibili.com/video/BV1Yb411a7ty?from=search&amp;seid=15239518141135081455。 hexo安装nodejs的下载和安装先下载并安装nodejs。nodejs是一种javascript的运行环境，它能够使得网络javascript脱离浏览器独立运行，而我们编写的hexo也需要nodejs来生成。具体细节因为博主也不太了解，所以也就不展开了。nodejs的下载网站如下：https://nodejs.org/en/ 进去之后网站应该会自动推荐适合你机器的版本，这里博主下载的是windowsx64的12.16.3版本。下载完成后直接点击安装并等待安装完成即可。安装完成后你的电脑中会多出Node.js和npm两个组件，在接下来会用到 利用npm安装hexo在完成Nodejs的安装后，可以打开终端并使用下面的命令查看版本，来确认是否安装完成：12node -vnpm -v如果正确安装，应该会各自打印出版本号。接下来，我们应该使用包管理器npm来进行hexo的安装，不过考虑到下载速度问题，建议使用国内的淘宝镜像源，过程如下： 先利用npm下载cnpm,并将镜像源指向淘宝1npm install -g cnpm --registry=https://registry.npm.taobao.org 查看cnpm的版本号来确定是否安装完成1cnpm -v 使用cnpm进行hexo博客的全局安装1cnpm install -g hexo-cli 此时可以看到各种插件的下载，等待安装完成后，使用hexo -v进行验证。 hexo的初始化和第一篇博文测试hexo初始化在自己喜欢的文件夹下新建一个blog文件加用于放置博客的相关文件并进入该文件夹，使用hexo命令进行博客的初始化：12mkdir blog &amp; cd bloghexo init如果提示需要管理员权限则可以管理员身份打开cmd进行操作(windows)或者在命令前加上sudo(Linux和MacOS)此过程需要一定的时间，等待完成后可以使用dir(windows)或ls(Linux和MacOS)命令查看一下blog文件夹下的文件，大概是下面这样： 上述文件是hexo为我们生成的博客框架，后续的操作，如新建博文等都需要在此框架下进行。我们可以使用hexo s命令来启动博客，然后在本地的4000端口进行访问，在浏览器输入http://localhost:4000进行访问：应该能得到如下的页面：这里博主已经将主题更换为Icarus主题了，初始的话应该是landscape的主题。并且博客在初始化时创建了一篇名为Hello World的文章介绍了hexo的相关命令。 第一篇博文测试使用如下命令创建一篇新的命令：1hexo new \"你的文章名字\"或1hexo n \"你的文章名字\"使用上述命令后会创建一篇你输入的文章名字的博文，该博文在source/_post文件夹下，是markdown格式。可以使用编辑器打开并进行修改,保存退出后使用hexo g命令重新生成博文，使用hexo s命令启动本地预览，然后在浏览器的url栏中输入http://localhost:4000, 进行预览，此时就能看到刚刚新创建的那篇博文了。 hexo的远端部署一种简单的且推荐的远端部署方式是部署到github上去。具体做法如下：先登录到自己的github账号，然后新建一个新的仓库，注意，用户部署个人博客的github仓库的命令必须符合一定的要求。必须是你的用户名+github.io的形式。如下所示：假设你的github用户名为xxx 点击”Create repository”完成仓库的创建。完成后将看到如下的信息： 我们先将这个仓库放在这里，回到命令行，使用cnpm安装一个git部署的插件,使用如下命令：1cnpm install --save hexo-deployer-git可能在安装过程中会遇到一些WARNING，忽略就好。安装完成后进行_config.yml的设置，该文件是整个hexo博客框架的设置文件。需要设置的条目在该文件的最下端：1234deploy: type: git repo: https://github.com/Holworth/xxx.github.io.git branch: master其中repo条目需要换成刚刚创建的那个仓库的地址。注意： 这里每个冒号之后都需要隔一个空格！！！ 完成设置后使用hexo d部署到远端，此时可能需要输入github的账号和密码，输入等待完成即可。此时刷新github仓库就会发现仓库中多了很多文件，在浏览器输入https://xxx.github.io 就可以对博客进行访问了。如果第一次访问发现是404 not found, 不要着急，等待一段时间再进行访问就可以了。 结语到目前为止，博客的搭建工作已经初步完成了，下一步就是进行更换主题等工作了。关于这个，我会另外写一篇简短的博文来总结一下更换主题的整个过程。而关于这个博客，我以后也会不定期地在其中加一些博文，包括但不限于一些计算机科学，技术，数学，人文方面的博文，还可能会加入我的一些随笔或想法。希望这次我能坚持下去，不要再半途而废。","link":"/2020/05/24/%E6%88%91%E7%9A%84%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/"},{"title":"正合列","text":"前言本文简要介绍模中的正合列以及短五引理。二者均是同调代数中的基本工具。这里主要简单地从抽象代数的角度来考虑这个问题 正合列一对模同态\\(f,g:A\\stackrel{f}{\\longrightarrow}B\\stackrel{g}{\\longrightarrow}C\\),如果满足\\(\\mathrm{Im}f=\\mathrm{Ker}g\\),则称\\(f,g\\)在\\(B\\)处正合。如果有一个有限长度的模同态链： A_0 \\stackrel{f_1}{\\longrightarrow} A_1 \\stackrel{f_2}{\\longrightarrow} ... \\stackrel{f_{n-1}}{\\longrightarrow} A_{n-1}\\stackrel{f_{n}}{\\longrightarrow} A_{n}满足下面的性质： \\mathrm{Ker}f_i=\\mathrm{Im}f_{i+1},\\forall i=1,2,..,n-1则称上述的模同态链为一个正合列 一些有趣的性质典范的嵌入和投影是正合的: 0\\longrightarrow A\\stackrel{\\iota}{\\longrightarrow}A\\bigoplus B\\stackrel{\\pi}{\\longrightarrow}B\\rightarrow 0此处0到\\(A\\)和B到\\(0\\)的映射都是显然的，所以不标出了。除此之外，设\\(C\\)是\\(D\\)的子模，则\\(C\\)到\\(D\\)的嵌入以及\\(D\\)到商模\\(D/C\\)的自然同态也是正合的： 0\\rightarrow C\\stackrel{i}{\\longrightarrow}D \\stackrel{p}{\\longrightarrow}D/C\\longrightarrow 0coimage和cokernel设模同态： f:A\\rightarrow B则\\(\\mathrm{Ker}f\\)是\\(A\\)的一个子模，称\\(A/\\mathrm{Kerf}\\)为\\(f\\)的coimage, 记为\\(\\mathrm{Coim} f\\)。\\(\\mathrm{Im}f\\)是\\(B\\)的一个子模，称\\(B/\\mathrm{Ker}f\\)为\\(f\\)的cokernel, 记为\\(\\mathrm{Coker} f\\).显然下列模同态是正合的： 0\\rightarrow \\mathrm{Ker}f\\rightarrow A\\rightarrow\\mathrm{Coim}f\\rightarrow 0 \\\\ 0\\rightarrow \\mathrm{Im}f\\rightarrow B\\rightarrow \\mathrm{Coker} f\\rightarrow 0 \\\\ 0\\rightarrow \\mathrm{Ker} f\\rightarrow A\\stackrel{f}{\\longrightarrow}B\\rightarrow\\mathrm{Coker}f\\rightarrow 0一个值得注意的地方是若: B\\stackrel{g}{\\longrightarrow} C\\rightarrow 0是正合列，则\\(g\\)是一个满同态。 短五引理(The Short Five Lemma)短正合列一个正合列如果具有以下形式，则称为短正合列(short exact sequence)： 0\\rightarrow A\\stackrel{f}{\\longrightarrow}B\\stackrel{g}{\\longrightarrow}C\\rightarrow 0短五引理短五引理 设\\(R\\)是一个环，下面的图是\\(R\\)-模的交换图：且满足：该交换图的每一行都是短正合列，则下列结论成立：若\\(\\alpha\\)和\\(\\gamma\\)都是单同态(满同态，同构)，则\\(\\beta\\)也是单同态(满同态，同构)","link":"/2020/05/28/%E6%AD%A3%E5%90%88%E5%88%97/"},{"title":"分式环与局部化","text":"引言分式环，或称为分式域，是包含环 \\(R\\) 的一个最小域，这一点，熟悉线性代数的人都不陌生。但我们知道，域作为环来看，一定是整环，因此，一个环有分式域，其前提必须是该环为整环。如果我们想对分式域的进行扩展，可以从环 \\(R\\) 的角度出发，在 \\(R\\) 非整环的条件下，考虑其关于一个乘性子集的“商”，这就是今天要讲的局部化，在该框架下，分式域也可作为局部化的一个特例。 局部化(localization)分式域的构造设 $R$ 是一个整环环我们定义 \\(\\mathbf{R}\\times \\mathbf{R}^{+}\\) 上的关系： (a,b)\\sim(c,d)\\Longleftrightarrow ad-bc=0则易于验证此关系是\\(\\mathbf{R}\\times \\mathbf{R}^{+}\\)上的一个等价关系，在此关系下的等价类\\(\\overline{(a,b)}\\)记作\\(a/b\\), 可以验证在下面的关于等价类的加法和乘法下所有等价类的集合构成一个整环： \\begin{aligned} a/b+c/d &= (ad + bc)/bd\\\\ a/b\\times c/d&=ac/bd \\end{aligned}其中，单位为\\(a/a,\\forall a\\in R^{+}\\), 零元为\\(0/a,\\forall a\\in R^{+}\\), 尽管这里写了\\(\\forall a\\in R^{+}\\),但实际上从等价类的角度看，它们是同一个元素。更为特别的，对于该环中的任何一个非零元 \\(a/d,a\\neq0\\)都有\\((a/d)^{-1}=d/a\\)从上面的讨论我们可以看出，该环实际上是一个域，称为 \\(R\\) 的\\(\\textbf{分式域}\\)。 局部化设 \\(R\\) 是一个环，\\(R\\) 的一个\\(\\textbf{乘性子集}\\) \\(S\\) 指的是 \\(R\\) 的一个非空子集，且满足： \\forall a,b\\in S, ab\\in S\\(\\textbf{定理1}\\) 设 \\(R\\) 为一个交换环，\\(S\\) 为\\(R\\) 的一个乘性子集，定义\\(\\mathbf{R}\\times \\mathbf{S}\\) 上的等价关系如下： (r,s)\\sim(r',s')\\Longleftrightarrow \\exist s_1\\in \\mathbf{S},\\mathbf{s.t}\\;\\;s_1(rs'-sr')=0则该关系是一个等价关系。\\(\\mathbf{R}\\times \\mathbf{S}\\) 在该等价关系下的等价类组成的集合记为 \\(S^{-1}R\\) \\(\\textbf{证明}\\) 易于验证该结论是成立的。\\(\\textbf{注}\\)： 当 \\(R\\) 无零因子且\\(0\\notin S\\)时，上述关系就等价于\\((r,s)\\sim (r’,s’) \\Longleftrightarrow rs’-r’s=0\\), 这正是我们在分式域的构造过程中建立的关系，从中可以看出分式域是局部化的一个特例。如同在分式域中那样，我们将 \\(S^{-1}R\\) 中的一个等价类\\(\\overline{(r,s)}\\)记为\\(r/s\\)。并且指出以下的事实： \\(tr/ts=r/s, \\forall r\\in R, s,t\\in S\\) 如果 \\(0\\in S\\), 则\\(S^{-1}R\\)由唯一的一个等价类组成，在此条件下,\\(\\forall (r,s),(r’,s’)\\in R\\times S\\),都有\\((r,s)\\sim (r’,s’)\\) \\(\\textbf{定理2}\\) \\(S^{-1}R\\)在如下定义的加法和乘法下构成一个带有单位元的交换环： r/s+r'/s'=(rs'+r's)/ss',\\;\\;\\; (r/s)(r'/s')=(rr'/ss')00\\(\\textbf{证明}\\) 对于这样的定义，我们首先必须证明的一点是该运算的结果与代表元的选取无关，即： (r_1,s_1)\\sim (r_1',s_1')\\Rightarrow r_1/s_1+r'/s'=r_1'/s_1'+r'/s'下面我们将证明这一点。若\\((r_1,s_1)\\sim (r_1’,s_1’)\\), 则根据定义，存在\\(s_2\\in S,s.t\\;\\; s_2(r_1s_1’-r_1’s_1)=0\\)，根据加法的定义有： r_1/s_1+r'/s'=(r_1s'+r's_1)/s_1s'以及 r_1'/s_1'+r'/s'=(r_1's'+r's_1')/s_1's'从而有 \\begin{aligned} s_2(s_1s'(r_1s'+r's_1)-s_1s'(r_1's'+r's_1'))&=s_2((s')^2(s_1'r_1-s'r_1'))\\\\ &=(s')^2(s_2(s_1'r_1-s'r_1'))\\\\ &=0 \\end{aligned}这表明该定义的结果与代表元的选择无关，类似地，可以对乘法也做相应的讨论，最终可以得出结论是，运算的结果与代表元的选择无关。从而等价类的运算归结为代表元的运算。在此结论下，不难验证\\(S^{-1}R\\) 确实是构成一个环。\\(S^{-1}R\\) 的交换性由 \\(R\\) 的交换性立即得到，其中\\(s/s， \\forall s\\in S\\) 都是 \\(S^{-1}R\\) 中的单位。\\(\\textbf{推论}\\) 针对\\(S^{-1} R\\), 当 \\(R\\) 和 \\(S\\) 具有一些特性时，\\(S^{-1} R\\) 也会有一些特性：(1). 若 \\(R\\) 无零因子，且\\(0\\notin S\\), 则 \\(R^{-1}S\\) 是一个整环。(2). 若 \\(R\\) 无零因子，且 \\(S\\) 是\\(R\\) 中所有非零元的集合，则\\(R^{-1}S\\) 是一个域\\(\\textbf{证明}\\)(1) 设 \\(r/s,r’/s’\\in R^{-1}S\\) 使得 \\((r/s)(r’/s’)=0\\), 即有 \\(rr’/ss’=0=0/s’’\\), 则 \\( \\exist s_1\\in S, \\mathbf{s.t}\\;\\; s_1(rr’s’’ - ss’\\times 0)=0\\), 从而有\\(s_1rr’s’’=0\\), 因为 \\(R\\) 是一个整环且 \\(0\\notin S\\), 从而 \\(rr’=0\\), 从而 \\(r=0\\) 或者 \\(r’=0\\), 从而 \\(r/s=0\\) 或 \\(r’/s’=0\\)。上述讨论说明了 \\(S^{-1}R\\) 是一个整环 (2) 当 \\(S\\) 是 \\(R\\) 中所有非零元组成的集合时，根据(1)有， \\(S^{-1}R\\) 是一个整环，且对任意 \\(r/s \\in S^{-1}R \\) 有 \\(r/s\\times s/r=rs/sr=s/r\\times r/s\\) 从而，\\((r/s)^{-1}=s/r\\), 注意这里 \\(r\\neq 0\\Rightarrow r\\in S\\Rightarrow s/r\\in S^{-1}R\\)。从而 \\(S^{-1}R\\) 是一个域，这就是 \\(R\\) 的分式域。 \\(\\textbf{注}\\) 对于一个非零的交换环 \\(R\\), 设 \\(S\\) 为其非零元非零因子组成的集合，则 \\(S\\) 为一个乘性子集，此时称 \\(S^{-1}R\\) 为 \\(R\\) 的完全分式环(\\(\\textbf{Complete ring of fractions}\\), 此处翻译可能有误)，特别地，当 \\(R\\) 是一个整环时，\\(S^{-1}R\\) 是一个域，称为 \\(R\\) 的分式域 局部化与 $R$ 的关系环\\(R\\)关于乘性子集\\(S\\)的局部化\\(S^{-1}R\\)与\\(R\\)有如下关系: 定义映射 \\(\\Phi_S:\\mathbf{R}\\rightarrow \\mathbf{S^{-1}R}, \\mathbf{r}\\mapsto \\mathbf{rs/s},\\forall s\\in \\mathbf{S}\\), 则不难验证该映射是一个\\(\\mathbf{R}\\)和\\(\\mathbf{S^{-1}R}\\)之间的同态。进一步地，我们有下面的定理： \\(\\textbf{定理3}\\)(1) 若\\(0\\notin \\mathbf{S}\\) 并且\\(\\mathbf{S}\\)不包含任何零因子，则\\(\\Phi_S\\)是一个单同态，特别地，对任何一个整环\\(\\mathbf{R}\\),\\(\\mathbf{R}\\)可嵌入它的分式域中(2) 如果\\(\\mathbf{R}\\)有单位1， 且\\(\\mathbf{S}\\)是由单位组成的，则\\(\\Phi_S\\)是一个环同构，，特别地，一个域的分式域同构于它自身。\\(\\textbf{证明}\\)(1) 我们只需证明\\(\\mathrm{ker}\\Phi_S={0}\\)即可，对任意\\(r\\in \\mathrm{ker}\\Phi_S\\), 有\\(rs/s=0=0/s_1\\)。从而存在\\(s’\\in \\mathrm{S}\\), 使得\\(s’rss_1=0\\), 由于\\(\\mathrm{S}\\)不含零因子也不含有0，从而只有可能是\\(r=0\\), 从而\\(\\mathrm{ker}\\Phi_S={0}\\), 从而该映射是一个单射。对于一个整环而言，定理中的\\(S^{-1}R\\)就是其分式域，上述单射保证了存在一个\\(R\\)到\\(S^{-1}R\\)的单同态，从而是一个嵌入映射。 (2)如果\\(\\mathbf{S}\\)中的任何一个元素都是单位，则显然\\(0\\notin \\mathbf{S}\\)并且\\(\\mathbf{S}\\)中不含有零因子，从而(1)的条件自然成立，根据(1)可知\\(\\Phi_S\\)是一个单同态，从而我们只需说明\\(\\Phi_S\\)是一个满射即可。注意到： \\forall r/s\\in S^{-1}R, \\Phi_S(rs^{-1})=rs^{-1}s/s=r/s从而\\(\\Phi_S\\)是一个满射，从而是一个同构。 下面的定理说明了分式环\\(S^{-1}R\\)在泛性质的意义下可由映射唯一地刻画。这个定理通常也被看作是分式环\\(S^{-1}R\\)的一个定义： \\(\\textbf{定理4(Universl Property)}\\)设 \\(R\\) 是一个交换环，\\(S\\) \\(R\\) 的一个乘性子集，设 \\(T\\) 是一个带有单位的交换环，若环同态 \\(f:R\\rightarrow T\\)满足：对任意 \\(r\\in S\\), 有\\(f(r)\\) 都是\\(T\\) 中的单位，则存在唯一的一个环同态\\(\\bar{f}:S^{-1}R\\rightarrow T\\)，使得\\(\\bar{f}\\Phi_S=f\\)。分式环\\(S^{-1}R\\)是由该性质唯一确定的 证明 定义\\(\\bar{f}:S^{-1}R\\rightarrow T, r/s\\mapsto f(r)f(s)^{-1}\\), 则不难验证这是一个环同态。且对于任何\\(r\\in R\\), 有\\(\\bar{f}\\Phi_S(r)=\\bar{f}(\\Phi_S(r))=\\bar{f}(rs/s)=f(rs)f(s)^{-1}=f(r)f(s)f(s)^{-1}=f(r)\\)从而 \\(\\bar{f}\\Phi_S=f\\)。下面证明唯一性：假设 \\(g:S^{-1}R\\rightarrow T\\) 也是满足上述条件的一个同态，则对任意的 \\(r/s\\) 有 \\(g(r/s)=g(\\Phi_S(r)\\Phi_S(s)^{-1})=g\\Phi_S(r)(g\\Phi_S(s))^{-1}=f(r)f(s)^{-1}=\\bar{f}(r/s)\\)而\\(bar{f}=g\\),这就证明了唯一性。至于定理后半部分的叙述，即\\(S^{-1}R\\)在同构意义下的唯一性，需要用到范畴论的部分知识，这里就暂时省略了","link":"/2020/05/24/%E5%88%86%E5%BC%8F%E7%8E%AF%E4%B8%8E%E5%B1%80%E9%83%A8%E5%8C%96/"},{"title":"RocksDB新型负载测试MixGraph源码分析","text":"前言本文将对发表于Fast’20上的Characterizing, Modeling, and Benchmarking RocksDB Key-Value Workloads at Facebook一文中的新型负载生成MixGraph函数的源码进行简单分析。 运行环境的配置通过阅读论文的细节和 github 上相应项目的 wiki 可以了解到，论文([3])的作者分析了三种使用情形下的负载之后，将：qps(query per second), 值的大小，扫描长度等等参数按一定的数学模型进行了拟合，从而得出了三种使用情形下负载的共同特点，并基于此构建出了模型。只需要改变模型的参数，就能使其适用于不同的情形。例如，wiki中给出了一组参数用于模拟ZippyDB使用情形。 论文([3])作者新实现了MixGraph Benchmark作为上述负载的模拟生成工具。调用方式和其他RocksDB的负载相同，但集成了更多的参数：$ax^b+cx^d$ 模型中的a,b,c,d用于每个键范围内访问次数的分布，$sin$ 分布模拟了qps等等。 版本变更： v6.0.1最早实现MixGraph， 但缺少了keyrange_dist_a等参数的声明(不知道是不是作者忘了) v6.8.1实现了上面提到的缺少参数的问题，但编译时失败 v6.8.1之后的版本编译均失败，错误提示疑似与类的继承有关。 最终决定使用v6.8.1版本 编译： 编译错误如下： 123/home/kqh/RocksDBv6.8.1-2/rocksdb/tools/db_bench_tool.cc: In constructor ‘rocksdb::NormalDistribution::NormalDistribution(unsigned int, unsigned int)’:/home/kqh/RocksDBv6.8.1-2/rocksdb/tools/db_bench_tool.cc:1519:58: error: declaration of ‘max’ shadows a member of 'this' [-Werror=shadow] NormalDistribution(unsigned int min, unsigned int max) : 查询了多方资料之后我认为是由于参数名字的问题导致的，覆盖了其基类的同名属性。所以将NormalDistribution中的参数min, max分别改为min_, max_, 并将接下来使用的对应位置也进行修改即可。完成上述修改后成功编译。 完成编译和安装后，运行wiki上给定的参考参数的mixgraph测试，可以正常运行，但从结果来看，IO带宽相比之前使用 RocksDB 自带的Benchmark 的运行, 如 readrandomwriterandom, fillseq等，只有原来的25%左右。 MixGraph运行流程总览通过阅读MixGraph的设计代码，发现其设计主要可以分为以下几个部分：初始化，包括生成prefix分布模型，基于prefix分布模型生成key, 如果参数选择不设置prefix模型则按照普通的随机数产生方式生成key；修改qps; 根据query type选择合适的数据库操作。 其中，prefix模型定义了两项指数分布，需要参数keyrange_dist_a, keyrange_dist_b, keyrange_dist_c, keyrange_dist_d等参数。该分布的意义在于模拟“热门” 键的局部性。qps服从于sin分布，其修改和初始化依赖于RateLimiter类，该类控制着读写的IO速率上限，从而控制了qps。 iterator scan和 val size 服从$f(x) = (1/\\sigma)(1+k(x-\\theta)\\sigma)^{(-1-1/k)}$ 分布，但我们只会在某一次具体操作时去生成该分布。 初始化12345678910111213int64_t read = 0; // including single gets and Next of iterators int64_t gets = 0; int64_t puts = 0; int64_t found = 0; int64_t seek = 0; int64_t seek_found = 0; int64_t bytes = 0; const int64_t default_value_max = 1 * 1024 * 1024; int64_t value_max = default_value_max; int64_t scan_len_max = FLAGS_mix_max_scan_len; double write_rate = 1000000.0; double read_rate = 1000000.0; bool use_prefix_modeling = false; read, gets, puts, found等变量的作用是用于记录对应的操作次数。value_max, scan_len_max记录的是用户要求最大的value size和 scan len。write_rate, read_rate正如前面所说，是用于初始化RateLimiter成员，从而控制IO速率。 12345678910111213141516GenerateTwoTermExpKeys gen_exp; std::vector&lt;double&gt; ratio{FLAGS_mix_get_ratio, FLAGS_mix_put_ratio, FLAGS_mix_seek_ratio}; char value_buffer[default_value_max]; QueryDecider query; RandomGenerator gen; Status s; if (value_max &gt; FLAGS_mix_max_value_size) { value_max = FLAGS_mix_max_value_size; } ReadOptions options(FLAGS_verify_checksum, true); std::unique_ptr&lt;const char[]&gt; key_guard; Slice key = AllocateKey(&amp;key_guard); PinnableSlice pinnable_val; query.Initiate(ratio); ratio记录了三种操作各自的占比，QueryDecider类型的query的作用是根据传入的三种操作(也可以是多种)的占比随机的生成操作类型，阅读QueryDecider的源码可以发现这个类的实现确实是按照各种操作占比的百分比生成操作类型的，这里就不展开了。gen和 gen_exp分别表示随机数产生器和一个服从指数分布的随机数产生器。PinnableSlice在这里似乎是没有什么作用的。 接下来需要初始化概率分布： 12345678// Decide if user wants to use prefix based key generation if (FLAGS_keyrange_dist_a != 0.0 || FLAGS_keyrange_dist_b != 0.0 || FLAGS_keyrange_dist_c != 0.0 || FLAGS_keyrange_dist_d != 0.0) { use_prefix_modeling = true; gen_exp.InitiateExpDistribution( FLAGS_num, FLAGS_keyrange_dist_a, FLAGS_keyrange_dist_b, FLAGS_keyrange_dist_c, FLAGS_keyrange_dist_d); } 如果二项指数分布的4个参数有一个不为零，则表明用户希望使用基于range的模型，设置相应的标志位即可。 生成查询key不采用prefix的模型在生成查询需要的key时就是使用简单的随机数方法，因此重点考察采用prefix模型的方法。 二项指数分布的初始化采用prefix的方法，概率密度分布服从二项指数分布：$ae^{bx}+ce^{dx}$ , 这里的x实际上指的是一个range的下标，一个range代表了在空间上紧密分布的键值对的集合。下面分析类GenerateTwoTermExpKeys: GenerateTwoTermExpKeys包含以下成员： 12345int64_t keyrange_rand_max_; // 随机数的最大范围int64_t keyrange_size_; // 一个range有多少个key-value对int64_t keyrange_num_; // 有多少个rangebool initiated_;std::vector&lt;KeyrangeUnit&gt; keyrange_set_; //保存每个range的概率值 其中最为核心的是keyrange_set_， 它保留了每个range的标号对应的概率值。在GenerateTwoTermExpKeys::InitiateExpDistribution的操作是，对每一个keyrange, 进行如下所示： 123456789for (int64_t pfx = keyrange_num_; pfx &gt;= 1; pfx--) { // Step 1. Calculate the probability that this key range will be // accessed in a query. It is based on the two-term expoential // distribution double keyrange_p = prefix_a * std::exp(prefix_b * pfx) + prefix_c * std::exp(prefix_d * pfx); if (keyrange_p &lt; std::pow(10.0, -16.0)) { keyrange_p = 0.0; } 上面的代码仅仅只是简单计算出了概率，后续还涉及放大的操作。所谓放大就是将0，1之间的概率密度映射到一个更大的区间内，其目的是用大的正数来代替很小的double型概率值，方便比较。作者的方法是采用 1 / (smallest key-range probability) 来作为扩大后区间的上界。由于指数函数本身是增函数(a, b, c, d &gt; 0)， 所以非零的第一项来计算即可： 123if(amplify == 0 &amp;&amp; keyrange_p &gt; 0) { amplify = static_cast&lt;int64_t&gt;(std::floor(1 / keyrange_p)) + 1;} 上述代码存在于for循环中，但由于刚刚解释的原因，amplify在第一次被更新后就不再为0，从而不会再实行if里面的语句。 之后创建一个KeyrangeUnit结构，KeyrangeUnit包含三个属性，分别是range的起始位置，range的size, 以及range的概率值(准确地说，这是一个概率的range，表示这个概率range的占用范围)。然后将这个结构插入到range_set里面即可： 1234567891011KeyrangeUnit p_unit;p_unit.keyrange_start = keyrange_start;if (0.0 &gt;= keyrange_p) { p_unit.keyrange_access = 0;} else { p_unit.keyrange_access = static_cast&lt;int64_t&gt;(std::floor(amplify * keyrange_p));}p_unit.keyrange_keys = keyrange_size_;keyrange_set_.push_back(p_unit);keyrange_start += p_unit.keyrange_access; 在上面的代码中，keyrange_access代表了概率密度对热门程度的关系。概率密度越大，意味着越容易被访问，从而是更加热门的key。反过来，为概率密度大的range设置更大的size, 使得对一个产生的随机数，更有可能落在这个range 范围中，从而更容易选择到这个range, 自然也体现了其热门程度。但是注意这里的keyrange_access和 range_size是不同的概念，前者是指一个range的访问次数，通过start + access的组合来标定其概率范围，后者是指真正的range空间中的key-value对的数目。通过代码也可以看出，每个KeyrangeUnit的 access属性都不一定相同，但是每个range的rangesize都是相同的，因为键空间是平均划分的。 二项指数分布生成key通过GenerateTwoTermExpKeys::DistGetKeyID可以根据二项指数分布生成一个key，输入包括了一个随机数(代表概率值）以及两个在range中随机化key的参数: 12int64_t DistGetKeyID(int64_t ini_rand, double key_dist_a, double key_dist_b) 首先根据 ini_rand，通过二分查找找到一个包含这个序号的range范围： 123456789while (start + 1 &lt; end) { int64_t mid = start + (end - start) / 2; assert(mid &gt;= 0 &amp;&amp; mid &lt; static_cast&lt;int64_t&gt;(keyrange_set_.size())); if (keyrange_rand &lt; keyrange_set_[mid].keyrange_start) { end = mid; } else { start = mid; }} 然后在这个range中随机找一个key： 1234567891011 int64_t key_offset = 0, key_seed; if (key_dist_a == 0.0 &amp;&amp; key_dist_b == 0.0) { key_offset = ini_rand % keyrange_size_; } else { key_seed = static_cast&lt;int64_t&gt;( ceil(std::pow((ini_rand / key_dist_a), (1 / key_dist_b)))); Random64 rand_key(key_seed); key_offset = static_cast&lt;int64_t&gt;(rand_key.Next()) % keyrange_size_; } return keyrange_size_ * keyrange_id + key_offset;} 这个随机数种子的产生方式从来没见过，不知道这么做的意义是什么。 修改QPS论文中提到QPS的分布呈现出sin函数分布的规律。即$qps =a\\sin(bx+c)+d$的分布，这里的x代表的是时间，准确地说是从该线程(基准测试)开始启动到当前运行的时间。 在代码中，关于修改qps的代码如下所示： 1234567uint64_t now = FLAGS_env-&gt;NowMicros(); uint64_t usecs_since_last; if (now &gt; thread-&gt;stats.GetSineInterval()) { usecs_since_last = now - thread-&gt;stats.GetSineInterval(); } else { usecs_since_last = 0; } GetSineInterval函数的返回值是上一次执行该操作时的时间，因此usecs_since_last的值是两次修改qps操作之间的时间间隔。参数FLAGS_sine_mix_rate_interval_milliseconds是由用户指定输入的用于设置时间间隔的参数，按照作者的说法，时间间隔越短，越能更好地拟合该分布。 接下来，先获取当前时刻到线程开始时刻的时间，作为求分布概率的横坐标, 同时记录本次操作的时间： 123double usecs_since_start = static_cast&lt;double&gt;(now - thread-&gt;stats.GetStart());thread-&gt;stats.ResetSineInterval(); 然后通过sin分布将时间间隔映射到一个分布值，并按照各种操作所占比例分配每个操作的速率，其中get和 seek操作都属于读操作，put属于写操作： 12345double mix_rate_with_noise = AddNoise( SineRate(usecs_since_start / 1000000.0), FLAGS_sine_mix_rate_noise);read_rate = mix_rate_with_noise * (query.ratio_[0] + query.ratio_[2]);write_rate = mix_rate_with_noise * query.ratio_[1] * FLAGS_mix_ave_kv_size; SineRate的计算方法很简单： 123double SineRate(double x) { return FLAGS_sine_a*sin((FLAGS_sine_b*x) + FLAGS_sine_c) + FLAGS_sine_d;} AddNoise的做法是在该点值的附近加上一个随机的数据，随机的数据分布在以原点值为中心，散布不超过原点值一定比例的区间中。 最后设置RateLimiter来达到控制速率的目的。 Pareto分布的使用在上面的生成查询key, 修改qps等工作完成之后就可以开始进行具体的操作了。MixGraph中只涉及了三类操作：Get, Put, Iterator, 除了三种操作都需要一个查询key之外，Put还需要一个value size的参数， Iterator还需要一个scan length的参数。它们都服从Pareto分布。而且是服从于参数不同的Pareto分布： 12345int64_t val_size = ParetoCdfInversion( u, FLAGS_value_theta, FLAGS_value_k, FLAGS_value_sigma);...int64_t scan_length = ParetoCdfInversion(u, FLAGS_iter_theta, FLAGS_iter_k, FLAGS_iter_sigma) % scan_len_max; Pareto分布的计算方式如下： 123456789int64_t ParetoCdfInversion(double u, double theta, double k, double sigma) { double ret; if (k == 0.0) { ret = theta - sigma * std::log(u); } else { ret = theta + sigma * (std::pow(u, -1 * k) - 1) / k; } return static_cast&lt;int64_t&gt;(ceil(ret));} 这个计算过程和wiki中作者描述的计算过程有一定的出入： f(x) = (1/\\sigma)*(1+k*(x-\\theta)\\sigma)^{(-1-1/k)}实际上，该函数描述的是一个给定$f(x)$， 求 $x$ 的过程，最终求解的结果为： x=\\theta+\\cfrac{(\\sigma f(x))^{-k}-1}{k\\sigma}貌似还是和函数的求解结果不完全一致，可能是作者经过多次测试对结果进行了微调。 参考资料[1] https://github.com/facebook/rocksdb/wiki/RocksDB-Trace,-Replay,-Analyzer,-and-Workload-Generation [2] https://www.cnblogs.com/pdev/p/11619881.html [3] Zhichao Cao, Characterizing, Modeling, and Benchmarking RocksDB Key-Value Workloads at Facebook, Fast’20","link":"/2020/09/06/RocksDB%E6%96%B0%E5%9E%8B%E8%B4%9F%E8%BD%BD%E6%B5%8B%E8%AF%95MixGraph%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"},{"title":"内存分配器简介","text":"前言本文是学习CSAPP第九章虚拟存储中关于动态内存分配一部分的资料总结。 动态内存分配简介（注：接下来谈到的内存分配除了特别说明都是指对虚拟内存地址的分配） 动态内存分配在程序设计中扮演着重要的角色。在广泛使用的虚拟内存结构的操作系统中，动态内存分配为程序员在必要情况下的实时内存需求提供了解决办法。尤其是对于某些在运行时刻才能知晓的内存使用，例如：一个程序要求用户输入一个数字，并创建相应大小的数组。显然该数组的具体大小在程序运行到用户输入之前是不可知的，更别提在栈中为其开辟相应的内存了。 C语言的动态内存管理以C语言为例，动态内存分配系统管理着虚拟内存分布中被称为“堆”的结构。堆的标记位置”brk”被内核记录，并通过系统调用brk()/sbrk()进行增加和减少。内存分配器，作为glibc实现的库函数的一部分，作用是在用户态为用户实现内存的管理。典型的使用方式是使用malloc()/free()这组库函数进行管理。 malloc()/free()的实现使用到了系统调用brk()/sbrk(), 以及mmap(), 据称，当malloc申请的大小小于128K时，会使用brk()增加堆空间(指的是堆的其他位置已经被分配了)；当大于128K时，则会使用mmap将一个交换文件映射到虚拟内存中，增加可用的动态内存。一些人认为程序能使用的虚拟内存大小是无限的或者受限于CPU的内存总线字长(32/64bits), 不过实际上还受限于操作系统交换分区的大小。 在malloc中使用brk()/mmap()系统调用的一个作用是修改内核保存的进程的页表结构。例如调用brk()将堆指针增大时，将堆新占据的虚拟页面设置为可用，这样当间接引用一个malloc返回的虚拟指针时才不会触发段错误。这也正是常说的：操作系统负责管理大的内存(4KB的页面)， 库函数管理小的内存(malloc分配字节粒度的内存)。 malloc分配的内存会有对齐处理，例如在32位系统中，要求返回的内存地址总是8的倍数；64位系统中则是16的倍数。当请求诸如5，19，11这种大小的内存时，malloc会返回对齐的地址，也会保证接下来剩余的空闲内存的开头地址也是8/16字节对齐的。可想而知，这样会实际上占用8/16整数倍大小的内存，从而导致内存碎片。 内存分配器模型要求和目标理论上，内存分配器必须满足下列要求： 能处理任意的请求序列，只要这个请求序列是满足一定的条件的，例如：释放的内存是前面通过该分配器请求的内存 能够立即相应请求，禁止通过缓存请求等手段提高分配器的性能 满足对齐要求，内存分配器堆内存的管理所设置的块大小必须满足一定的要求，以保证能对齐。 内存分配器的目标包括以下两个： 最大化吞吐率： 最大化吞吐率要求内存分配器能尽快处理内存请求，包括申请和释放。换言之，内存分配器要有合理的数据结构和算法，提高性能 最大化内存利用率：内存利用率可以用堆内有效载荷 和 堆的总大小 的比值来衡量。所谓堆内有效负载就是指用户请求分配的内存大小， 由于碎片，记录信息等结构的存在，以及内核管理页表的具体实现等原因， 内存利用率不可能是100%。 内存碎片碎片可以理解为一种内存无法被使用而导致利用率低下的现象。例如上面提到的对齐要求，如果我们按照8字节对齐，并且仅仅只要求分配5个字节的内存，此时为了满足对齐要求，我们会连续分配8字节的内存，这将导致有3个字节的内存处于无法使用的状况。 一般来说，内存碎片有两种： 内部碎片：就是指分配的内存比请求的有效内存大导致的的碎片。这部分碎片几乎没办法使用，就像上面提到的对齐要求中的碎片一样 外部碎片：指空闲内存因为位置上的不连续导致无法满足一个外来的申请请求的现象。例如：内存中的空闲内存总共有24Bytes，此时用户请求了15Bytes，从数字上来看是可以分配的。但实际上空闲的24Bytes内存没有相邻，导致无法将这些内存分配给用户，只能继续通过brk向内核申请内存 如果用户允许分配的内存不是连续的，或许能解决碎片问题，但是这将导致用户无法使用简单的指针运算来使用内存, 例如： 12int *new_ptr = (int *)malloc(n * sizeof(int));*(new_ptr + n / 2) = 1; 如果malloc返回的是不连续的内存，上述第二条语句可能会导致一个Segment Fault。 内存分配器的实现简单模型将虚拟内存视为一个N字节的数组，使用一个指针p来指向堆的顶端，在有分配需求时增加指针。这样简单的模型为我们提供了下面的实现： 1234567891011121314static char *heap_;static char *heap_max_;void *malloc(size_t increment) { void *old = (void *)heap_; if (heap_ + increment &gt; heap_max_) { fprintf(stdout, \"allocate error\"); return (void *)(-1); } heap_ += increment; return old;}void free() { return;} 不难看出，这样的实现无法有效利用内存，被置于heap_之后的内存，即使被释放也无法再次被使用。不过这样的实现足够简单，会有较高的吞吐率， 不过内存利用率非常低下。 链表模型上述实现的不足之处在于：没有区分内存的状态(使用和空闲), 没有管理空闲内存，没有对内存进行回收再次利用。 针对上面的问题，我们可以采用链表模型：将空闲内存标记为使用和空闲两类，并采用链表将它们联系起来。为此我们需要一些约定的术语： 一个块指的就是连续的被分配或空闲的内存整体； 一个chunk指的也是连续的内存片，通常认为chunk是通过brk()/mmap()之类的系统调用向操作系统申请的更大规模的内存片。 接下来，我们将先详细讲解隐式空闲链表的相关内容，再简要介绍显式和分离式模型。 隐式空间链表基本结构每种内存分配器都需要一定的数据结构来记录内存使用情况，隐式链表通过一个称为头部的结构记录一个块的信息。 假定内存管理按照8字节对齐，这样分配的内存地址的低3位始终是0。为了记录一个块占用的内存大小，我们只需要记录该32位数值的高29位，低3位作为控制信息，例如记录是否分配。这样，一个块的头部可以使用1个word，即4个bytes的内存进行记录。一个块的大小包含了：头部大小，申请的有效内存大小，以及用于8字节填充的内存大小。如下图所示： 可以通过第一个头部遍历整个堆区域，原因是通过第一个头部可以得到第一个块占用的内存大小，将指针加上这个大小就能得到第二个头部，如此进行下去即可。这也是该结构被称为隐式链表的原因，因为并没有明显的指针来索引下一个位置。一个简单的采用隐式空闲链表的内存分布如下图所示： 分配内存通过遍历堆中所有的块，找到一个可以满足申请内存请求的空闲堆并将该块的内存位置返回即可。 查找满足申请需求的空间时有3种适配策略： 首次适配：每次从头开始找，返回遇到的第一个合适的空闲块。 下一次适配：从上一次结束搜索的位置开始找，返回遇到的第一个合适的空闲块。 最佳适配：扫描整个链表，返回空闲块大小和需求最适合的空闲块(例如使浪费最少)。 首次适配会倾向于在链表开头处就进行分配，从而导致在链表靠前的位置留下较多的碎片，并且导致后面的位置空闲块较多；下一次适配则会在整个链表空间上留下分配块；最佳适配有三种策略中最高的内存利用率，但它将会扫描整个链表空间找到最佳匹配，需要更长的运行时间。 找到的空闲块通常会有大于申请空间大小的内存，直接将该空闲块完全分配给用户可能会导致碎片。但对于最佳适配策略而言这或许是一个可以接受的做法；但对于其他适配策略，更好的做法是将空闲块拆分为分配块和另一个空闲块。 内存回收采用头部标记的内存块回收是比较简单的，只需要将头部的分配位修改为0即可。但是这样的回收策略仍然可能导致分配时找不到合适的空闲块而失败。更好的办法是将该块与其相邻的空闲块进行合并。 合并的策略有两类： 立即合并：块被释放后就立即与相邻的空闲块合并 推迟合并：在某个内存申请找不到合适的空闲块时执行合并。 立即合并在内存申请和释放频繁的情况下会执行多次，但由于立即合并只考虑相邻的空闲块，所以能够在常数时间内完成。推迟合并通常需要遍历整个链表再决定合并，时间与链表长度成正比。但立即合并存在的一个问题是抖动，即一个内存分配后又立即释放，然后又进行分配，如此反复执行。这种特殊的程序行为将导致内存分配器在合并相邻空闲块之后又被迫拆分空闲块，在释放后又进行合并。 合并方法 假设当前释放块为[Ak], 当释放后，它将读取[A{k+1}]的头部(通过[Ak]头部加上[A_k]头部中包含的块大小信息)，如果[A{k+1}]头部显示该块为空闲的，则在[Ak]头部中加上[A{k+1}] 块的大小即可。 这种合并方法很简单，但是它只能合并释放块之后的空闲块，对于之前的空闲块只能采用遍历链表的形式来找到。为了解决这一问题，Knuth在块的末尾又加上了头部的副本，被称为脚部， 这种带有头部和脚部的组织方式被称为边界标记法。(块大小也要同时包含头部和脚部的大小)下图展示了一个采用边界标记法的内存块的组织形式： 通过脚部，我们将[Ak]的头部减去[A_k] 的块大小就能得到[A{k-1}]的头部，之后修改[A_{k-1}]头部中的块大小信息即可。值得注意的是，在这种合并方法中，先合并后面的空闲块，再合并前面的空闲块 。 通常考虑一个释放块与它前后的相邻块的空闲/占用情况时会有四种情况，下图展示了这四种情况下合并内存的结果：其中头部和脚部的标志位a代表被分配，f代表空闲。 显式空闲链表显式空闲链表可以认为是在隐式空闲链表上的一个优化。在隐式空闲链表中，由于缺少快速的索引手段，我们在搜索一个空闲块时需要扫描整个链表，包括已分配块以及空闲块。在显式空闲链表中，使用显式的指针prev, succ分别表示一个空闲块的前驱空闲块以及后继空闲块，从而让我们能快速索引到一个可分配的空闲块。 显示空闲链表的一个问题在于回收，回收时一种简单的策略是将释放的块重新插入到整个空闲链表的头部，这种策略被称为后进先出(LIFO), 但是问题在于这种方式无法有效地对相邻的空闲块进行回收，因为相邻的空闲块在链表中可能并不相邻。另一种改进的方式则是采用地址大小排序的方式，即要求插入一个块时，其前驱占用的内存地址必须比自己小。这种方式能在释放的同时进行合并，但是也要求在插入空闲块时必须遍历整个空闲链表来找到正确的前驱位置。 显然，上述两种回收方式分别具有较大的吞吐量（执行时间短）和较大的内存利用率(内存碎片少)。 分离空闲链表分离链表的想法是将具有不同大小的空闲块组织在不同的链表中，这样在分配以及回收时只需要到对应大小的空闲链表中搜索即可。分离存储维护多个不同的链表，每个链表都有大致相等的块（并不要求每个链表中大小都相同，可以是某个范围。）并且链表数组按照大小的升序进行排列。一般的分离存储策略有以下几种： 分离适配分配器维护一个空闲链表的数组，每个空闲链表与一个大小范围相关联。每个链表包含了大小不同但处于该范围内的块。 当一个分配请求到达时，首先确定该分配请求应该位于的范围，然后在该链表中采用首次适配的方法搜索，如果找到了一个，则可以选择将该空闲块分割，将剩余的空闲部分插入到另一个空闲链表中；如果在该链表中找不到则去下一个链表中寻找。 GNU malloc采用了这种方法，因为理论上它同时具有较好的使用效率以及内存利用率：在一个合适的链表中进行首次适配的效率和在所有空闲块中进行最佳适配的内存利用率相当。 一种分离适配的改进方法被称为二层分离适配(Two Level Segregated Fit, TLSF), 它使用两层链表，第一层记录的大小类范围为2的幂次，例如：[2, 4], [4, 8],…,[2048, 4096]等等。第二层则是在第一层的基础上，对每个范围进行平均的线性划分，例如[2048, 4096]这个范围被划分为8个相同大小的小区间：[2048, 2304], [2304, 2560]…, 每个区间都具有相同的大小。同时，每层还采用位图的方式来记录每个区间内是否有空闲块，加快了搜索效率。 伙伴系统伙伴系统是一种特殊的分离存储形式，它的每个链表的空闲块大小被限制为2的幂次。当分配一个大小为 m 的块时，先找到离它最近的大于它的2的幂次，然后在对应的链表中将整个块分给它。 在回收时，伙伴系统采用一种高效的合并方式，对于一个大小为$2^k$的块，如果有另一个大小为$2^k$ 的块，并且它们在内存地址上是相邻的：xxx…x0000和xxx…x1000, 那么就可以将它们合并为一个大小为$2^{k+1}$大小的块，并插入大小为$2^{k+1}$的链表中。 伙伴系统一个明显存在的问题就是碎片问题。在分配一块内存时，采用找到大于所需内存的最小2的幂次的方法将导致分配内存中存在内部碎片，一个极端的例子是请求分配$2^k+1$的内存，此时只能为其分配一个大小为$2^{k+1}$的空闲块。而且这些多余的碎片在位置上并不连续，又将导致外部碎片。 参考资料[1]《深入理解计算机系统》 [2] http://dmitrysoshnikov.com/compilers/writing-a-memory-allocator/#segregated-list-search","link":"/2020/10/03/%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D%E5%99%A8%E7%AE%80%E4%BB%8B/"},{"title":"Fundamental Theorem of Galois Theory(1)","text":"前言本文简要介绍一些关于Galois Theory的基本入门知识，包括Galois Group的定义，以及基本定理。关于基本定理的证明，本文将先证明一系列引理，然后将在下一篇文章中进行基本定理的完整证明。","link":"/2020/10/12/Fundamental-Theorem-of-Galois-Theory-1/"},{"title":"tcmalloc总体结构分析","text":"前言tcmalloc是一种经典的内存分配器，在多线程并发的情况下有较好的表现。本文尝试从总体来解读tcmalloc的大致结构和使用流程，并加入一些简单的源代码方便理解。 (注：这里所提到的tcmalloc指的是gperftools中的tcmalloc, 而不是github上google/tcmalloc仓库中的tcmalloc， 两者有巨大的区别，首先前者支持全操作系统平台，后者仅仅只支持Linux操作系统。后者在实现的接口上也与前者不同，没有采用malloc/free接口。tcmalloc的使用可以参考资料(2)) 总体结构tcmalloc的总体结构可以分为三部分：ThreadCache, CentralFreeList, PageHeap。 ThreadCache是一个线程局部存储对象。也就是说线程并不需要和其他线程竞争ThreadCache内部数据的使用权，也不会在分配时加锁。 CentralFreeList是一种中间数据结构，它的作用是处于PageHeap和ThreadCache之间，调度两个部分内存使用。当ThreadCache内存不足时，就会从CentralFreeList申请内存，CentralFreeList则会从PageHeap申请内存。 PageHeap是专门用于管理大块内存的结构，它管理的内存单位是在page这个量级。PageHeap是一个全局共享的数据结构，在使用时需要加锁。它负责与操作系统进行交互，申请或回收内存，也向CentralFreeList提供或回收内存。 总体结构的示意图如下所示:(此处的图参考了资料[1]的讲解图，但又有所不同) ThreadCacheThreadCache采用分离式存储的方法，通过链表数组的形式来实现。同一链表中的内存块大小相同，不同链表中的内存块大小则不同，一个内存块被称为object。具有相同内存块大小的链表被称为class, 按照我看的多篇资料的说法，一共有86个不同的size class, 但实际上，根据当前下载的gperftools的版本，该值是96，定义在common.h中： 1static const size_t kClassSizeMax = 96 FreeListThreadCache组织这些Size Class的方法如下：在ThreadCache内部定义了一个FreeList类，该类代表一个链表，然后使用FreeList的数组表示所有的Size Class。代码如下： 12345678910111213141516class ThreadCache { ...private: class FreeList { private: void* list_; uint32_t length_; uint32_t lowater_; uint32_t max_length_; uint32_t length_overages_; int32_t size_; //记录该链表保存的每个obejct的size }; FreeList list_[kClassSizeMax]; ...}; 上面的代码中关于FreeList实现的部分中，void是索引到下一个内存块的指针，其他的属性，例如length_, lowater_之类的是整个链表的元数据(64位机器使用32位数据)，用于记录信息。ThreadCache中定义的list_成员表示了所有size class。 FreeList并没有采用双向指针，而是采用单向的索引，因为按照tcmalloc的做法，分配内存时只会从头部读取一个内存块。而且值得注意的是，void指针占用了每个object开头8个字节的空间，但是在malloc返回该内存块后，这个指针占用的八个空间是可以被指针使用的。可能的疑问是 free* 的时候怎么知道该内存块的长度？这个暂时还不清楚。 FreeList 的插入和删除元素的相关函数如下所示，其中箭头代表调用： 123FreeList::Push(void *ptr) -&gt; SLL_Push(void **, element*) -&gt; SLL_SetNext(void *t, void *n)FreeList::Pop() -&gt; SLL_pop(void **) 阅读相关源码不难发现，Push操作和Pop操作都是在整个链表的表头做相应的操作。 AllocateAllocate函数是ThreadCache的核心函数之一，其函数原型如下： 1inline ATTRIBYTE_ALWAYS_INLINE void* ThreadCache::Allocate(size_t size, uint32_t cl); 其中参数size表示要分配的内存数，cl表示应该从哪个size class中分配，cl的值代表了该size class 在FreeList list_中的index。 Allocate函数中采用ASSERT断言进行一些检查，其中一项为ASSERT(size &lt;= kMaxSize), kMaxSize的值定义在文件common.h中，为256 * 1024， 表明ThreadCache所能分配的最大内存字节数为256KB。 Allocate函数会先尝试从FreeList数组中拿出对应的内存块，如果失败则从前面提到的CentralFreeList中取得： 123if (!list-&gt;TryPop(&amp;rv)) { return FetchFromCentralCache(cl, size);} FetchFromCentralCache暂时不仔细分析，它的作用大概就是从CentralFreeList中取出一系列的object然后加入到ThreadCache的FreeList中。 内存申请ThreadCache中有一条语句： 1const int batch_size = Static::sizemap()-&gt;num_objects_to_move(cl); 这条语句说明需要向CentralFreeList申请多少内存。当向ThreadCache申请内存而ThreadCache的FreeList中并没有剩余的内存时，ThreadCache向CentralFreeList申请内存，但并不只是申请用户所需要的那么多内存，此时ThreadCache具体申请的内存数量由一个被称为num_objects_to_move 的数组决定。 该数组(连同其他两个具有相似作用的数组)被定义在class SizeMap中，Static::sizemap()将返回一个class SizeMap的指针(这个SizeMap是全局的，还是线程私有的?)，并调用对应的映射函数。class SizeMap定义在common.h中，几个对应函数的定义如下： 1234567891011121314151617181920class SizeMap { ... private: int num_objects_to_move_[kClassSizeMax]; int32 class_to_size_[kClassSizesMax]; size_t class_to_pages[kClassSizesMax]; public: inline int32 class_to_size(uint32 cl) { return class_to_size_[cl]; } inline size_t class_to_pages(uint32 cl) { return class_to_pages_[cl]; } inline int num_objects_to_move(uint32 cl) { return num_objects_to_move_[cl]; } ...}; num_objects_to_move 表示当ThreadCache向CentralFreeList申请内存时应该将多少内存给ThreadCache(单位是object), class_to_pages 表示当CentralFreeList内存也不够，需要向PageHeap请求内存时，应该给CentralFreeList多少内存(单位：Page, tcmalloc的page是8KB的) 各个 size class 对应的上述数值到底是多少，可以参考资料[1] More既然讲到了class SizeMap, 也可以说一说ThreadCache::Allocate(size_t size, uint32_t cl)中的cl是哪里来的。 Allocate的调用发生在do_malloc函数中， do_malloc可以认为是整个tcmalloc提供的向外的接口。 1ATTRIBUTE_ALWAYS_INLINE inline void* do_malloc(size_t size); do_malloc中采用调用： 12345678910111213do_malloc(size_t size) { ... uint32 cl; if (PREDICT_FALSE(!Static::sizemap()-&gt;GetSizeClass(size, &amp;cl))) { return do_malloc_pages(cache, size); } size_t allocated_size = Static::sizemap()-&gt;class_to_size(cl); if (PREDICT_FALSE(cache-&gt;SampleAllocation(allocated_size))) { return DoSampledAllocation(size); } return CheckedMallocResult(cache-&gt;Allocate(allocated_size, cl));} do_malloc采用的调用顺序是先看是不是大页的分配，如果是的话，就调用do_malloc_pages, 然后才轮到ThreadCache的分配。 do_malloc_page的调用参数虽然是ThreadCache类型的但是可以通过ThreadCache结构内部的一个指针引用到对应的一个PageHeap上，直接进行页级别的分配。 大内存分配：PageHeapPageHeap 结构用于管理大内存的分配。其管理大内存的数据结构被称为Span(span.h), 一个Span值得就是一大块连续的页面。 SpanSpan的机构如下： 12345678910111213struct Span { PageID start; //typedef uintptr_t PageID Length length; //typedef uintptr_t Length Span* next; Span* prev; void* objects; unsigned int refcount : 16; unsigned int sizeclass : 8; unsigned int location : 2; unsigned int sample : 1; enum { IN_USE, ON_NORMAL_FREELIST, ON_RETURED_FREELSIT };}; 可以看出Span也是以双向链表的形式进行组织的，还有一项void *objects的作用是形成类似size class里面的objects的单向链表，因为一个span到达CentralFreeList之后，会将这个Span切割成多个大小相同的objects(取决于size class的大小)，方便给ThreadCache分配。 最后一行定义了一个span的三种状态，这个留到PageHeap里面介绍。 PageHeap中Span的组织PageHeap结构中定义了如下结构： 1234567struct SpanList { Span normal; Span returned;};SpanList free_[kMaxPages];SpanList large_; 简单来说，PageHeap采用了和size class 中管理objects相似的办法。通过链表数组来管理所有Span。占用相同页面数量的Span被放置在一个SpanList中，共有kMaxPages个不同的size class, 这里kMaxPages经过计算大概是1 &lt;&lt; 7 = 128。 PageHeap还有一个被称为large_的SpanList ,按照注释的说明，当一个Span占用的页面大于kMaxPages时这个Span就会被放置到这个SpanList中。 SpanList内部还有两个链表：normal和returned，这两个链表各自代表什么意思我还不太清楚。 中间结构CentralFreeListCentralFreeList在PageHeap 和ThreadCache中间充当了内存传递的结构。在前面一直没有说明的一点是：所有ThreadCache的某个size class 类都共享同一个CentralFreeList, 这决定了在向CentralFreeList申请内存时必须加锁。 CentralFreeList的数据结构记录了以下几点： 12345size_t size_class_; //表明该CentralFreeList被什么size class共享Span empty_;Span nonempty_;size_t num_spans_;size_t counter_; 两个Span分别记录了empty的span和nonempty的span，empty是指该span中的所有objects都被ThreadCache使用了，nonempty则相反。 ThreadCache从CentralFreeList中获得指定大小页面的函数接口为： 1int CentralFreeList::FetchFromOneSpans(int N, void **start, void **end); 其中N表示需要的object的数量，返回值是实际提供给ThreadCache的object的数量。start和end分别表示了这次分配的object链表的起始和终点位置。 FetchFromOneSpansSafe是更加严谨的版本，会先检查是否有足够的内存进行分配，如果不行，则向PageHeap申请内存后再进行分配。 FetchFromOneSpansSafe被CentralFreeList中的函数：RemoveRange调用，而RemoveRange又在ThreadCache的FetchFromCentralCache中被调用，从而完成了一次内存申请。 归还objects的接口为： 12void ReleaseListToSpans(void *start);void ReleaseToSpans(void *object); 以上函数都需要当前线程获得锁才能执行，函数带有EXCLUSIVE_LOCKS_REQUIRED(lock_)的后缀。 CentralFreeList向PageHeap申请Span的函数调用是 1void CentralFreeList::Populate() 这个函数从PageHeap中获得Span的页面后会对页面进行分解，拆分成多个相同size的objects。 参考资料[1] tcmalloc总体原理讲解 [2] gperftools/tcmalloc简单使用 [3] tcmalloc源码分析","link":"/2020/10/12/tcmalloc%E6%80%BB%E4%BD%93%E7%BB%93%E6%9E%84%E5%88%86%E6%9E%90/"}],"tags":[{"name":"KV Store","slug":"KV-Store","link":"/tags/KV-Store/"},{"name":"RocksDB, LSM-Tree","slug":"RocksDB-LSM-Tree","link":"/tags/RocksDB-LSM-Tree/"},{"name":"数学，代数，抽象代数","slug":"数学，代数，抽象代数","link":"/tags/%E6%95%B0%E5%AD%A6%EF%BC%8C%E4%BB%A3%E6%95%B0%EF%BC%8C%E6%8A%BD%E8%B1%A1%E4%BB%A3%E6%95%B0/"},{"name":"RocksDB","slug":"RocksDB","link":"/tags/RocksDB/"},{"name":"数据库，工具使用， RocksDB","slug":"数据库，工具使用，-RocksDB","link":"/tags/%E6%95%B0%E6%8D%AE%E5%BA%93%EF%BC%8C%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8%EF%BC%8C-RocksDB/"},{"name":"数据结构, 高速存储","slug":"数据结构-高速存储","link":"/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84-%E9%AB%98%E9%80%9F%E5%AD%98%E5%82%A8/"},{"name":"操作系统, 30天自制操作系统","slug":"操作系统-30天自制操作系统","link":"/tags/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F-30%E5%A4%A9%E8%87%AA%E5%88%B6%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"},{"name":"操作系统，30天自制操作系统","slug":"操作系统，30天自制操作系统","link":"/tags/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%EF%BC%8C30%E5%A4%A9%E8%87%AA%E5%88%B6%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"},{"name":"博客, 技术, 工具","slug":"博客-技术-工具","link":"/tags/%E5%8D%9A%E5%AE%A2-%E6%8A%80%E6%9C%AF-%E5%B7%A5%E5%85%B7/"},{"name":"RocksDB, 源码分析, 负载生成","slug":"RocksDB-源码分析-负载生成","link":"/tags/RocksDB-%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90-%E8%B4%9F%E8%BD%BD%E7%94%9F%E6%88%90/"},{"name":"内存，操作系统","slug":"内存，操作系统","link":"/tags/%E5%86%85%E5%AD%98%EF%BC%8C%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"}],"categories":[{"name":"存储","slug":"存储","link":"/categories/%E5%AD%98%E5%82%A8/"},{"name":"数据库","slug":"数据库","link":"/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"},{"name":"数学","slug":"数学","link":"/categories/%E6%95%B0%E5%AD%A6/"},{"name":"工具","slug":"工具","link":"/categories/%E5%B7%A5%E5%85%B7/"},{"name":"数据结构","slug":"数据结构","link":"/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"},{"name":"RocksDB","slug":"数据库/RocksDB","link":"/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/RocksDB/"},{"name":"操作系统","slug":"操作系统","link":"/categories/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"},{"name":"代数","slug":"数学/代数","link":"/categories/%E6%95%B0%E5%AD%A6/%E4%BB%A3%E6%95%B0/"},{"name":"三十天自制操作系统","slug":"操作系统/三十天自制操作系统","link":"/categories/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E4%B8%89%E5%8D%81%E5%A4%A9%E8%87%AA%E5%88%B6%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"},{"name":"内存","slug":"存储/内存","link":"/categories/%E5%AD%98%E5%82%A8/%E5%86%85%E5%AD%98/"},{"name":"存储引擎","slug":"存储/存储引擎","link":"/categories/%E5%AD%98%E5%82%A8/%E5%AD%98%E5%82%A8%E5%BC%95%E6%93%8E/"}]}